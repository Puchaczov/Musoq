# Current Work Unit (2026-09-12) — Structured input remediation Wave 13

- Wave 13 closes the independent qualification population and fixes a planner defect found by it: typed structural consumers of `UNION ALL` CTEs now receive one planner-owned generated row representation, while ordinary relational set-operation consumers retain `Row`. The regression `Sql_UnionAllCteUsesOneTypedStoredRowRepresentationForStructuralArguments` checks the plan, generated C#, execution result, and one source construction.
- Added deterministic, uniquely hashed populations: 2,000 parser/recovery cases (seed `0x5A17_20`, 1,600 valid and 400 invalid), 512 binder/type/default cases (seed `0xB17D_20`), and 128 separately compiled execution cases across four input origins, eight CTE operators, four execution modes/contexts (seed `0xC7E_20`). Coverage cells, authored diagnostics, shrink candidates, and metamorphic checks are asserted; all focused population tests pass.
- Refreshed Q344 and `GeneratedCodeSamplesManifest.txt` through the ignored refresh utilities. Repeated CTE, scalar-source, and VALUES BenchmarkDotNet ShortRun cohorts; raw reports are under `BenchmarkDotNet.Artifacts/structured-input-wave13-{cte,scalar,values}` with hashes recorded in the qualification baseline.
- Final unchanged-tree Release gate from the Wave 13 tree: Schema 556, Parser 2,694, Plugins 4,743, Git 45, CSV 49, Structured Inputs 51, Benchmarks 106, Converter 1,163, Evaluator 11,885; 21,292 passed, 3 intentional generated-sample refresh skips, 0 failures. Runner `--all` passed all authored resources and native probes. TRX files are recorded in the remediation ledger; all Wave 13 files are staged and ready for the single commit.

# Current Work Unit (2026-09-12) — Structured input remediation Wave 12

- Wave 12 now qualifies actual generated execution entry points rather than manual `StructuralValue` conversion. The benchmark covers inline, retained `let`, host capture, typed CTE adaptation, nested and nullable benchmark-only sources, empty input, source/result phases, scalar sources, and parenthesized VALUES across sizes 1, 3, 32, 1,024, and the near-limit 19,999-record cohort. The boundary host cohort contributes 99,995 record/field nodes, within one five-node record stride of the 100,000-node budget; oversized generated SQL literals are deliberately avoided.
- Generated-shape qualification resolves hot roots from Execution IR constructor callables, traverses generated callees and approved evaluator helpers, and rejects boxing, reflective array access, reflection invocation, delegates, iterators, per-record dictionaries, and object-packed conversion paths. Benchmark source scans prove the cohorts contain no `StructuralValue` trees or manual conversion helpers. Focused benchmark qualification currently passes 8/8 after correcting the near-limit stride assertion.
- Completed BenchmarkDotNet ShortRun cohorts with raw ignored JSON/CSV artifacts under `BenchmarkDotNet.Artifacts/structured-input-wave12-*`; the reproducibility record contains environment, commands, representative means/allocations, and SHA-256 report hashes. Environment: Windows 11 build 26200, Intel Core Ultra 9 285K/24 cores, SDK 10.0.303, runtime 10.0.11, BenchmarkDotNet 0.15.8. Full Release validation and runner remain the final steps before the single Wave 12 commit `perf(evaluator): qualify generated structural execution`.

# Current Work Unit (2026-09-12) — Structured input remediation Wave 9

- Wave 9 makes CTE adaptation consume planner-owned generated row shapes and records ownership, lifetime, and metrics strategy on each structural CTE input. Primitive and record CTE helpers now use direct typed row fields; a typed metrics prepass runs before target allocation and includes receiver defaults, limits, cancellation, empty-input handling, and atomic publication. Transfer is permitted only for an exact uniquely owned representation; records remain fresh constructed values.
- Removed renderer-side stored-row shape discovery for structural CTE preparation and the outer generic structural-limit guard for CTE inputs. Fingerprints, plan output, IR rewrites, and generated samples carry the CTE demand metadata. Added CTE limit, planner ownership, and generated-shape regressions; refreshed Q338–Q346, Q361–Q362, Q364, and Q367 plus the manifest.
- Focused evaluator, CTE, IR, generated-shape, and structured-input tests pass. The final unchanged-tree Release gate, runner, summary refresh, and one Wave 9 commit remain required.

# Current Work Unit (2026-09-12) — Structured input remediation Wave 8

- Receiver structural limits now travel from the selected typed-source signature through logical/physical source bindings into execution preparation. Inline, inferred/declared `let`, and host primitive-array roots are guarded at the actual source-demand boundary with generated typed walkers; checks count collection/record/scalar/null nodes, maximum depth, and checked UTF-16 string bytes while preserving cancellation.
- Added `MQ7013` runtime limit guards with origin/path/source-context facts and retained the specific diagnostic envelope when a source-open boundary wraps the failure. Primitive array parameters retain their legacy public contract but use the typed sequence read for structural demand checks. Fingerprints, plan printing, IR rewriting, and generated helper members include limit bindings.
- Added a maintained regression proving `let` and parameter inputs fail before `StrictLimitSource` construction, alongside the existing inline limit and constructor-failure checks. Generated snapshots/manifests were refreshed, and lazy iterator implementations that masquerade as indexed lists are rejected during host capture. Focused evaluator/target/example builds and receiver/runtime tests pass; the final Release gate, runner, summary refresh, and one unchanged-tree commit remain required.

# Current Work Unit (2026-09-12) — Structured input remediation Wave 7

- Wave 7 replaces generated host collection access through the old reflective element helper with exact typed array/list branches, covariant reference-list access, typed scalar reads, rank/contract validation, cached case-insensitive field-name lookup, and one-pass packed duplicate validation. The obsolete `GetCollectionElement`/stable-list reflection path is removed; Q337 and Q360 plus the tracked generated manifest were refreshed, and focused shape/runtime/benchmark checks pass.
- Wave 7 adds evaluator regressions for typed collection counts, rank and element-contract rejection, direct case-insensitive record presence, duplicate detection, and generated samples free of reflective element access. Runner `--all` passed all 35 positive/native probes. A Release build completed with 0 warnings/errors; the evaluator suite passed 11,881 tests with 3 intentional refresh skips and the other solution projects passed Schema 556, Parser 2,694, Plugins 4,743, Git 45, CSV 49, Benchmarks 105, and Converter 1,163. The structured-input test results are recorded as 45/45 in the latest project TRX under `src/dotnet/examples/data-sources/structured-inputs/tests/TestResults`; the parallel test host does not terminate after reporting completion on this machine and was stopped after its results were emitted.
- Final tree review, summary update, and one unchanged-tree Release gate remain required before committing `perf(evaluator): eliminate boxed host capture paths`.

# Current Work Unit (2026-09-12) — Structured input remediation Wave 6

- Wave 6 adds `StructuralInputMetrics`, the `MQ7013` limit exception contract, per-root and execution-global host metrics, checked constant-default premeasurement, collection lower-bound checks, and missing-field preservation for optional fields without declaration defaults. Generated structural parameter capture now reserves complete default subtree metrics and checks collection lower bounds before allocation.
- Added evaluator regressions for present/null/default/absent node accounting, UTF-16 string bytes, multiple-root global node budgets, constant-default rejection before materialization, checked metric overflow, cancellation, and stable limit diagnostic facts. Refreshed Q337, Q356, and Q360 generated samples plus the tracked manifest through the refresh utilities; refresh `[Ignore]` attributes were restored.
- Focused evaluator tests pass 21/21 and the complete solution gate passed Schema 556, Parser 2,694, Plugins 4,743, Git 45, CSV 49, Structured Inputs 45, Benchmarks 105, Converter 1,163, and Evaluator 11,879 with 3 intentional refresh skips (21,279 passed, 3 skipped, 21,282 total). The evaluator TRX is `src/dotnet/Musoq.Evaluator.Tests/TestResults/Jakub_DESKTOP-I52018A_2026-09-12_10_40_13_net10.0.trx`; the other project TRX files are in their corresponding `TestResults` directories from the 10:40 gate. The structured-input runner `--all` passed all 35 positive resources and native probes. The final unchanged-tree Release gate must be rerun after this evidence update before committing `fix(evaluator): capture complete structural parameter metrics`.

# Current Work Unit (2026-09-12) — Structured input remediation Wave 5

- Wave 5 finalizes portable structural planning: recursive execution shapes, explicit preparation limits/origin/metrics/ownership metadata, planner-owned CTE row representations, and IR/host ABI migration to 5/6. Typed stored-table discovery now runs in evaluator plan construction after scope and optimization boundaries; C# rendering consumes the immutable `ExecutionPlan.StoredTableRepresentations` map. Older IR 4/ABI 5 requests receive explicit unsupported-version diagnostics.
- Updated direct renderer fixtures to provide planner-owned typed CTE representations, refreshed the 17 affected generated structural samples and `GeneratedCodeSamplesManifest.txt`, and kept the semantic compilation cache identifier-independent.
- Final Wave 5 Release validation passed with 0 build warnings/errors. The complete solution suite passed Schema 556, Parser 2,694, Plugins 4,743, Git 45, CSV 49, Structured Inputs 45, Benchmarks 105, Converter 1,163, and Evaluator 11,872 with 3 intentional refresh skips (21,272 passed, 3 skipped, 21,275 total). Evaluator TRX: `src/dotnet/Musoq.Evaluator.Tests/TestResults/Jakub_DESKTOP-I52018A_2026-09-12_10_19_26_net10.0.trx`; the other project TRX files are in their corresponding `TestResults` directories from the same 10:19 gate. The structured-input runner `--all` passed all 35 positive resources and native probes. The unchanged tree is ready for commit `refactor(engine)!: finalize structural preparation contracts`.

# Current Work Unit (2026-09-12) — Structured input remediation Wave 4

- Structural source preparation now retains every authored scalar field in source order and emits `ExecutionPrepareStructuralInput` for receiver construction; nested records, arrays, and conversions retain their recursive authored ordering before constructor-order argument mapping.
- Added the preparation target to execution declared-variable facts so extracted CTE helper methods do not capture preparation locals as parameters. Scoped preparation names now include the source-row scope, preventing generated C# collisions between outer and CTE helper methods. Structural default fallback rendering uses the actual constructor parameter type.
- Added a generated-C# regression for reordered record fields and kept the correlated source-order assertion. Focused evaluator IR and structured-input tests pass; the required unchanged-tree Release gate and runner remain to be run before the Wave 4 commit.

# Current Work Unit (2026-09-12) — Structured input remediation Wave 3

- Typed source binding now evaluates scalar and complete-CTE interpretations across the complete typed overload set before selecting a candidate. Cross-overload ambiguity reports MQ3116 with scalar/relation candidate facts, and a resolved incompatible CTE cannot fall back to an identifier or string conversion.
- Candidate binding reuses one argument-to-parameter mapping, preserves the most specific mapping/structural/scalar failure, and carries candidate/path facts instead of discarding conversion errors. CTE scalar interpretation uses only an explicitly resolved visible scalar-column type; collection-valued scalar columns remain supported.
- Added evaluator regressions for cross-overload ambiguity, incompatible CTE preservation, and structural failure details. Schema conversion coverage now rejects invented element-wise array conversions while retaining nullable, decimal, and char rules. Focused binder and classifier tests pass 18/18; the full Release gate and structured-input runner remain required before commit.

# Current Work Unit (2026-09-12) — Structured input remediation Wave 1

- Reopened the structured-input qualification contract in `specs/musoq-core-language-spec.md` and `specs/structured-inputs-test-ledger.md`. The ledger now marks the audited semantic, limit, planner, ownership, and performance requirements pending instead of claiming completion; the parenthesized VALUES migration remains implemented.
- Recorded a current-HEAD baseline on `8abf6c068105b010e80fb10d081a994b408c867b` with a clean worktree before the documentation change. Release restore/build completed with 0 warnings/errors. The full suite passed Schema 551, Parser 2,694, Plugins 4,743, Git 45, CSV 49, Structured Inputs 44, Benchmarks 105, Converter 1,163, and Evaluator 11,866 with 3 intentional refresh skips (21,260 passed, 3 skipped, 21,263 total). The structured-input runner `--all` passed all 35 positive resources and native probes. TRX files are under each project's `TestResults` directory from the 09:00 gate.
- Remaining work is the 12 remediation waves: canonical conversion, cross-overload binding, authored evaluation order, portable structural plans, complete host metrics, unboxed capture, receiver and CTE limits, ownership cleanup, sample/example refresh, actual generated performance qualification, and independent populations.

# Current Work Unit (2026-09-12) — Structured input remediation Wave 2

- Wave 2 aligns `SchemaConversionClassifier` with the existing `MethodsMetadata` conversion tables. Nullable lifting is directional (`T` to `T?` is permitted; `T?` to `T` is rejected), decimal and char widening use the established costs, and nullable array conversion cannot erase element nullability.
- Added four Schema regressions for nullable direction, decimal/char costs, and nullable arrays. Focused tests pass 4/4. The tree is ready for the required full Release gate before commit `fix(schema): restore canonical source conversion costs`.

## Current Work Unit (2026-09-12) — Structured input remediation Wave 15

- Wave 15 closes qualification gaps with independent populations and authored expectations. Parser cases now carry expected code, phase, source origin, span bounds, diagnostic facts/edit validation, normalized SHA-256 query hashes, coverage-cell counters, and a deterministic failure-only shrinker. Binder cases now require unique normalized structural hashes and cover presence/default/null state cells. The compiled execution population now compiles 128 distinct query texts, records normalized hashes and coverage cells, and uses independently built expected rows.
- Added same-artifact typed concurrency coverage with four different per-run host values, equivalent array/list/StructuralValue host snapshots, fresh mutable receiving objects per invocation, artifact fingerprint stability across host and CTE result changes, authored literal/default invalidation, and explicit previous Execution IR rejection. The specification and remediation ledger now mark all listed requirements implemented pending the final validation evidence.
- Focused Wave 15 checks pass. The Release restore/build completed with 0 warnings/errors; the complete suite passed Schema 551, Parser 2,694, Plugins 4,743, Git 45, CSV 49, Structured Inputs 44, Benchmarks 105, Converter 1,163, and Evaluator 11,866 with 3 intentional refresh skips (21,260 passed, 3 skipped, 21,263 total). The evaluator TRX is `src/dotnet/Musoq.Evaluator.Tests/TestResults/Jakub_DESKTOP-I52018A_2026-09-12_02_31_22_net10.0.trx`; the other project TRX files are in their corresponding `TestResults` directories from the same gate. The structured-input runner `--all` passed all 35 positive resources and native probes. The final unchanged-tree gate must be rerun after this evidence update before the single Wave 15 commit.

## Current Work Unit (2026-09-12) — Structured input remediation Wave 14

- Wave 14 replaces the nonnegative allocation assertion with warmed typed-versus-structural allocation cohorts at sizes 1, 3, 32, and 1,024, separately measures generated result allocations, and verifies equal results with no structural allocation excess. The benchmark now exposes scalar-source and parenthesized-VALUES cohorts plus a 99,999-node pattern shape (one node below the 100,000 global budget).
- Generated hot-method qualification now resolves inline method call targets, traverses generated callees, and rejects boxing, reflective array access, reflection/Activator calls, delegate construction/invocation, iterator access, and dictionary use in structural helpers. Generated-shape checks also reject retired structural factories and reflective conversion text while permitting the documented host-boundary StructuralValue metadata.
- Focused performance qualification passes 7/7; the BenchmarkDotNet catalog lists all 17 preparation and cohort methods. The Release restore/build completed with 0 warnings/errors; the complete solution suite passed Schema 551, Parser 2,694, Plugins 4,743, Git 45, CSV 49, Structured Inputs 42, Benchmarks 105, Converter 1,160, and Evaluator 11,866 with 3 intentional refresh skips (21,255 passed, 3 skipped, 21,258 total). Evaluator TRX: `src/dotnet/Musoq.Evaluator.Tests/TestResults/Jakub_DESKTOP-I52018A_2026-09-12_02_04_47_net10.0.trx`. The structured-input runner `--all` passed all 35 positive resources and native probes. The final gate must be rerun after this summary update before the Wave 14 commit.

## Current Work Unit (2026-09-12) — Structured input remediation Wave 13

- Wave 13 adds remediation-focused generated samples Q354-Q368 for typed source overloads, declaration defaults, explicit null metadata, lifecycle/context identity, mutable ownership, typed parameter snapshots, recursive and parallel direct CTE arguments, receiver demand, filtered CTEs, mixed VALUES/array syntax, overload inventory, and empty inputs. The catalog and shape tests now cover the complete Q330-Q368 structured-input family, and the tracked manifest plus snapshots were refreshed through the repository utilities.
- The sorted CTE duplicate was replaced with a filtered direct CTE argument so the new sample does not introduce table-backed stored-row access; assertions require typed preparation and metadata-only descriptions. The generated corpus count is now 366 and mutable-source shape checks use the fully qualified generated type.
- Focused generated-code shape, corpus, duration, and maintainability checks pass 308/308 and the evaluator test project builds with 0 warnings/errors. The Release restore/build completed with 0 warnings/errors; the complete solution suite passed Schema 551, Parser 2,694, Plugins 4,743, Git 45, CSV 49, Structured Inputs 42, Benchmarks 104, Converter 1,160, and Evaluator 11,866 with 3 intentional refresh skips (21,254 passed, 3 skipped, 21,257 total). Evaluator TRX: `src/dotnet/Musoq.Evaluator.Tests/TestResults/Jakub_DESKTOP-I52018A_2026-09-12_01_49_57_net10.0.trx`. The structured-input runner `--all` passed all 35 positive resources and native probes. The final gate must be rerun after this summary update before the Wave 13 commit.

## Current Work Unit (2026-09-12) — Structured input remediation Wave 12

- Wave 12 expands the maintained structured-input example with numeric/structural overloads, declaration-versus-constructor default conflict, source-context identity, strict-limit, mutable ownership, throwing-source, and scalar/CTE ambiguity fixtures. Shared positive resources and runner manifest entries cover the new sources; invalid resources cover overload ambiguity, MQ3116 scalar/CTE ambiguity, strict limits, and constructor failures.
- Added a Schema-owned visible-column lookup for complete CTE arguments so a bare CTE name that is also a valid scalar column reports MQ3116 instead of silently choosing the relation. The fixture uses a collection-valued column and a record CTE, preserving the no relation-to-single-record rule.
- Focused structured-input tests pass 42/42. Final Release restore/build passed with 0 warnings/errors; the complete suite passed Schema 551, Parser 2,694, Plugins 4,743, Git 45, CSV 49, Structured Inputs 42, Benchmarks 104, Converter 1,160, and Evaluator 11,851 with 3 intentional refresh skips (11,854 total). Evaluator TRX: `src/dotnet/Musoq.Evaluator.Tests/TestResults/Jakub_DESKTOP-I52018A_2026-09-12_01_24_30_net10.0.trx`. The structured-input runner `--all` passed all 35 positive resources and the native probes. The summary update requires the final gate to be rerun before the Wave 12 commit.

## Current Work Unit (2026-09-12) — Structured input remediation Wave 11

- Wave 11 completes structural argument description and overload metadata remediation. Declared field defaults are now materialized as present values during evaluator normalization, so declaration defaults take precedence over receiving constructor defaults. Explicit-null defaults use canonical SQL text `null`; absent defaults remain a database-null metadata cell through the Schema-owned formatter.
- Typed source registration descriptions preserve every deterministic registered overload and concrete DESC carries the selected overload index. Canonical literal formatting is centralized in Schema, and the obsolete evaluator formatter was removed. Added evaluator/schema regressions for default presence, explicit-null description text, overload inventory, and canonical escapes.
- Focused evaluator and Schema tests pass. The final Release restore/build completed with 0 warnings/errors; the complete suite passed Schema 551, Parser 2,694, Plugins 4,743, Git 45, CSV 49, Structured Inputs 41, Benchmarks 104, Converter 1,160, and Evaluator 11,851 with 3 intentional refresh skips (11,854 total). Evaluator TRX: `src/dotnet/Musoq.Evaluator.Tests/TestResults/Jakub_DESKTOP-I52018A_2026-09-12_00_57_35_net10.0.trx`. The structured-input runner `--all` passed all 29 resources. `git diff --cached --check` passed before the gate; the summary update requires one final gate rerun before commit.

## Current Work Unit (2026-09-12) — Structured input remediation Wave 10

- Wave 10 removes the boxed `StructuralInputRuntime` conversion and delegate-based CTE preparation path. Structural CTE arguments now render one stable, typed preparation helper per shape with direct indexed field reads, nested construction, cancellation checks, and atomic completion; shared CTE call sites reuse the generated helper without sharing provider-facing arrays.
- Updated `ExecutionCSharpRenderer` structural conversion to reject any unlowered fallback, refreshed Q338-Q346 and the manifest, and moved benchmark/corner-case assertions to test-only typed inspection. Added production scans proving the retired runtime, `PrepareRows`, `Array.GetValue`, and delegate factory paths are absent from structural target code.
- Initial full validation caught the renderer-family maintainability budget at 28,514/28,513; the registration call was split into the existing line budget without weakening the guard. The focused maintainability suite then passed 66/66.
- Final Wave 10 Release restore and build passed with 0 warnings/errors. The complete suite passed Schema 550, Parser 2,694, Plugins 4,743, Git 45, CSV 49, Structured Inputs 41, Benchmarks 104, Converter 1,160, and Evaluator 11,848 with 3 intentional refresh skips (11,851 total); Evaluator TRX: `src/dotnet/Musoq.Evaluator.Tests/TestResults/Jakub_DESKTOP-I52018A_2026-09-12_00_35_12_net10.0.trx`. The structured-input runner `--all` passed all 29 resources.
- Wave 10 commit is being closed as `fix(evaluator): harden structural ownership and failures`; next step is Wave 11 complete structural descriptions and overload metadata, including declared-default preservation and the corrected source-overload contract.

## Current Work Unit (2026-09-12) — Structured input remediation Wave 9

- Wave 9 requires a planner-provided typed row shape for every structural CTE argument. `TypedStoredTableResultResolver` now ignores only unresolved shape observations while rejecting conflicting concrete generated row types, allowing shared and parallel CTE consumers to use the authoritative typed slot.
- Structural CTE rendering now resolves the typed slot shape when the expression was lowered without an attached shape and fails clearly if planning did not provide one. The `Row` source type, indexed `row[...]` reads, and structural CTE fallback are no longer emitted. VALUES lowering maps generated qualified fields back to authored names so independent parallel CTE definitions remain correct.
- Q340 and Q346 generated samples were refreshed. Q346 now contains two independent VALUES CTEs passed directly to `match` and `numbers` under `Parallel.Invoke`; shape guards reject `PrepareRows<Musoq.Evaluator.Tables.Row>` and indexed structural reads. Added an example execution-mode regression for direct parallel CTE-to-source arguments.
- Focused checks passed: evaluator structured sample/maintainability filters 70/70, generated sample shape checks 4/4, and structured examples 41/41. `git diff --check` passed.
- Final Wave 9 Release restore and build passed with 0 warnings/errors. The complete suite passed Schema 550, Parser 2,694, Plugins 4,743, Git 45, CSV 49, Structured Inputs 41, Benchmarks 104, Converter 1,160, and Evaluator 11,847 with 3 intentional refresh skips; Evaluator TRX: `src/dotnet/Musoq.Evaluator.Tests/TestResults/Jakub_DESKTOP-I52018A_2026-09-12_00_05_54_net10.0.trx`. The structured-input runner `--all` passed all 29 resources.
- Next step is stage/review and commit `feat(planner): require typed cte storage for structural arguments`, then begin Wave 10 removal of obsolete runtime conversion and delegate paths.

## Current Work Unit (2026-09-11) — Structured input remediation Wave 8

- Wave 8 rejects contextual record and array literals when a datasource has only legacy reflection metadata, preventing structural calls from silently falling back to legacy/object dispatch. The diagnostic remains the existing no-matching-overload envelope and explains that explicit typed source registration is required.
- Added semantic binding near misses for mixed record/scalar arrays, nested array scalar mismatches, non-nullable null fields, and untyped structural source calls. Existing structural declaration, default, casing, constructor, CTE ambiguity, and collection-contract coverage remains green.
- Focused binder and diagnostic checks passed 27/27. Final Release restore/build passed with 0 warnings/errors; the complete suite passed Schema 550, Parser 2,694, Plugins 4,743, Git 45, CSV 49, Structured Inputs 40, Benchmarks 104, Converter 1,160, and Evaluator 11,847 with 3 intentional refresh skips. Evaluator TRX: `src/dotnet/Musoq.Evaluator.Tests/TestResults/Jakub_DESKTOP-I52018A_2026-09-11_23_46_24_net10.0.trx`. Runner `--all` passed all 29 resources; `git diff --check` passed.
- Next step is commit the tested Wave 8 tree as `fix(evaluator): reject untyped structural source fallback`, then begin typed CTE representation remediation.

## Current Work Unit (2026-09-11) — Structured input remediation Wave 7

- Wave 7 implements generated typed host-parameter preflight. Compiled runnables with structured declarations now implement `IStructuralParameterSnapshotProvider`; all compiled, typed, and context execution entry points invoke the provider before datasource execution, with the legacy snapshotter retained for scalar-only artifacts and wrappers forwarding the provider.
- Added host-boundary normalization with active-ancestor cycle detection, checked depth/node/string budgets, supported stable arrays/lists and string-keyed dictionaries, explicit null handling, cancellation, atomic publication, and rejection of lazy enumerators, invalid keys, negative counts, and unsupported shapes. Declared field defaults are materialized as present values so they override receiving constructor defaults.
- Generated capture methods use typed carrier storage, packed presence words, direct indexed collection loops, and one boundary dictionary; generated samples Q335, Q336, Q337, and Q352 plus the manifest were refreshed. Added declaration-default precedence coverage and updated performance/shape guards for the deliberate boundary dictionary.
- Focused checks passed: evaluator generated/manifest/maintainability filters 424 passed with 1 intentional refresh skip; structured-input foundation and host-boundary tests passed; benchmark performance qualification passed 6/6. `git diff --check` passed.
- Final Wave 7 Release restore and build passed with 0 warnings/errors. The complete suite passed Schema 550, Parser 2,694, Plugins 4,743, Git 45, CSV 49, Structured Inputs 40, Benchmarks 104, Converter 1,160, and Evaluator 11,843 with 3 intentional refresh skips; the evaluator TRX is `src/dotnet/Musoq.Evaluator.Tests/TestResults/Jakub_DESKTOP-I52018A_2026-09-11_23_39_11_net10.0.trx`. Runner `--all` passed all 29 resources.
- Next step is commit the tested Wave 7 tree as `feat(evaluator): capture structural parameters into typed carriers`, then continue Wave 8 invalid-declaration and binding hardening.

## Current Work Unit (2026-09-11) — Structured input remediation Wave 6

- Wave 6 implementation is complete in the working tree: retained structural lets now emit private readonly typed carrier structs with packed `ulong` presence words; nested records/arrays recurse through typed storage; receiver adapters preserve missing-field presence and apply declaration defaults before receiving constructor defaults; primitive arrays transfer without an avoidable copy.
- Q335, Q336, and Q352 generated samples were refreshed through the repository utilities. Q336 now proves a declaration default (`Mode = 'regex'`) overrides the receiving constructor default (`"literal"`); Q352 uses 65 optional fields and asserts two presence words. Added retained nested-record/array and declaration-default execution regressions to the maintained structured-input datasource tests.
- Target-side CLR metadata access for the carrier compatibility path is confined to the existing `ScriptParameterSyntaxFactory` boundary so the portable execution guardrail remains satisfied. The structural carrier file contains no direct CLR sidecar property access.
- Focused carrier/shape/manifest tests passed 21 with 1 intentional refresh skip; structured-input examples passed 39; the full Release solution build passed with 0 warnings/errors; the complete suite passed Schema 550, Parser 2,694, Plugins 4,743, Git 45, CSV 49, Structured Inputs 39, Benchmarks 104, Converter 1,160, and Evaluator 11,843 with 3 intentional refresh skips; runner `--all` passed all 29 resources. `git diff --check` passed.
- The Wave 6 tree is validated but not yet committed. Next step is stage/review and commit `feat(evaluator): lower literals and lets to typed structural carriers`, then continue Wave 7 host-parameter preflight.

## Current Work Unit (2026-09-11) — Structured input remediation Wave 5

- Wave 5 source preparation now evaluates authored structural fields/elements in source order into typed locals, then lets constructor-order rendering consume those locals. Loop-invariant insertion recognizes reserved structural preparation locals as a boundary so correlated field reads are initialized first.
- Typed source opening now creates one `SourceExecutionContext` local and wraps source construction plus `OpenTypedRowSource` in one generated lifecycle boundary. Cancellation and existing lifecycle exceptions are preserved; constructor/open failures are wrapped with source identity.
- Added a correlated example regression covering generated ordering, one context construction, execution, and one source construction. Extracted structural preparation and typed-source declaration helpers into focused collaborators to keep maintainability families below their frozen totals.
- Refreshed Q330-Q349 and Q352 generated samples and the manifest through the ignored refresh utilities; updated the structural sample guard for typed preparation output.
- Final Wave 5 Release build passed with 0 warnings/errors. Full suite passed Schema 550, Parser 2,694, Plugins 4,743, Git 45, CSV 49, Structured Inputs 37, Benchmarks 104, Converter 1,160, Evaluator 11,843 with 3 intentional skips; runner `--all` passed all 29 resources. Next step is commit the tested Wave 5 tree, then remediate declared-default preservation and typed carrier storage in Wave 6.## Current Work Unit (2026-09-11) — Structured input remediation Wave 4

- Wave 4 in progress: introduced deterministic typed source candidate binding with Schema-owned conversion costs, scalar/CTE ambiguity diagnostic MQ3116, and source constructor stable IDs/callable references carried through logical, physical, and Execution IR bindings.
- Bumped portable contract versions to Execution IR 4 and host ABI 5; updated guardrail and machine-readable diagnostic catalog.
- Added focused binder coverage for exact numeric selection, equal structural ambiguity, and scalar/CTE ambiguity (9/9 focused tests).
- Release build passed with 0 warnings/errors. Full solution test gate passed after isolating an overlapping IDE test-session race: Parser 2,694, Schema 550, Plugins 4,743, Git 45, CSV 49, Structured Inputs 36, Evaluator 11,843 executed/passed with 3 intentional skips; runner `--all` passed all 29 resources.
- Legacy method-resolution scoring was restored after a confirmed generic-array regression; the new classifier remains limited to structural source binding until equivalent generic-array rules are implemented.
- Current tree has uncommitted Wave 4 changes; next step is stage/review/commit `fix(evaluator): bind structured source overloads deterministically`, then begin Wave 5 evaluation-order and lifecycle fixes.
## Current Work Unit (2026-09-11) — Complete structured inputs and VALUES migration

- Implemented the twenty-wave Core plan on branch feature/runtime_v2_final. The final language uses parenthesized VALUES rows and contextual array { ... } with parenthesized named records; the former brace-row VALUES spelling is rejected as a breaking change.
- Final Wave 20 commit is 06354c3f3 (test(engine): close structured input corner cases). It adds deterministic fixed-seed populations of 2,000 parser/recovery cases, 512 binding/type/default cases, and 128 compiled execution cases, with valid near misses, malformed syntax, field-order/casing/default/host-representation metamorphic checks, and observable array-order checks.
- Specification version is 1.7.0 and marked Implemented. Added docs/structured-inputs.md and updated the structured-input test ledger. The maintained Core example datasource and Q330-Q353 generated sample corpus remain current.
- Final Release restore and build passed with zero warnings/errors. The complete solution suite passed Schema 546, Parser 2,692, Plugins 4,743, Git 45, CSV 49, Structured Inputs examples 36, Benchmarks 104, Converter 1,160, and Evaluator 11,840; 3 intentional generated-sample refresh tests were skipped and no tests failed. TRX files are under each project's TestResults directory, with the final evaluator run at src/dotnet/Musoq.Evaluator.Tests/TestResults/Jakub_DESKTOP-I52018A_2026-09-11_11_04_17_net10.0.trx.
- The structured-input runner completed all 29 authored resources, including native source checks, parenthesized VALUES, CTE inputs, correlated/coupled calls, and every DESC family. Wave 19 focused performance/IL qualification passed 6/6; the representative BenchmarkDotNet artifact is ignored under BenchmarkDotNet.Artifacts.
- No push, tag, publication, or other remote action was performed. The final worktree is clean after the Wave 20 commit.
# Copilot Session Summary

## Current Work Unit (2026-09-06) — Ignore campaign documentation

- Added `docs/campaign` to the root `.gitignore`.
- `git diff --check` and `git check-ignore -v docs/campaign` passed.
- No tests were run because this is a gitignore-only change. No commit or remote action was performed.

## Current Work Unit (2026-09-02) — Published Core 17.0.9-alpha.1
- Corrected the CI `-AllPackages` alpha policy in `scripts/release/Release.Common.ps1`, `scripts/release/Pack-Release.ps1`, and `scripts/release/Test-ReleaseScripts.ps1`; documented the policy in `RELEASING.md`; committed and pushed it as `6828c6a3d4eded44305ef0a1978a1492901813d8` without moving the immutable tag.
- Repaired CI run `33676224911` completed green after rerunning Linux primary: restore, build, warning ratchet, full tests, and package packing passed on Linux; Windows validation also passed.
- Reran the existing tag-triggered Publish run `33673335071` against `v17.0.9-alpha.1` at `f736dbae25679b4e34d4d6ae8ff70ad776f10156`. Both `Validate and pack` and `Publish to NuGet` completed successfully.
- GitHub Release `v17.0.9-alpha.1` is published as a non-draft prerelease with exactly ten assets: `.nupkg` and `.snupkg` for `Musoq.Parser`, `Musoq.Plugins`, `Musoq.Schema`, `Musoq.Evaluator`, and `Musoq.Converter`.
- The release watcher verified all five exact NuGet versions after indexing, and final flat-container HEAD checks returned HTTP 200 for every package. Release URLs: `https://github.com/Puchaczov/Musoq/releases/tag/v17.0.9-alpha.1` and `https://github.com/Puchaczov/Musoq/actions/runs/33673335071`.
- Final focused release-script tests passed. The earlier full Release solution gate remains green at 19,755 passed, 3 intentional generated-sample refresh skips, and 0 failures. The remote tag resolves to the release commit; the branch is at `6828c6a3d4eded44305ef0a1978a1492901813d8`.
- Final worktree audit found and preserved three inserted timeout lines in `.github/workflows/ci.yml` and `.github/workflows/publish.yml`; these remain uncommitted local changes and were not discarded.
- Release work is complete. Any remaining action is separate review/commit of the local workflow timeout changes.

## Current Work Unit (2026-09-02) — Fix CI alpha pack policy for 17.0.9-alpha.1
- Authorized and fixed the Linux CI failure without moving the immutable release tag. `Pack-Release.ps1 -AllPackages` now evaluates each package's `Version` and skips datasource ABI validation only for alpha package versions; preview, beta, and stable versions still validate against the baseline.
- Added `Test-AlphaReleaseVersion` regression coverage for alpha, preview, and stable versions and documented the all-packages policy in `RELEASING.md`.
- Local CI-equivalent all-packages packing and clean-consumer smoke passed. Restore, the warning-clean Release build, and the complete solution test gate passed with 19,755 tests passed, 3 intentional generated-sample refresh skips, and 0 failures.
- The release tag `v17.0.9-alpha.1` remains at `f736dbae25679b4e34d4d6ae8ff70ad776f10156`; the policy fix is a follow-up branch commit and must not be used to move the tag. Next steps are push/confirm Linux CI, rerun Publish run `33673335071`, and verify GitHub Release plus all five NuGet versions.

## Current Work Unit (2026-09-02) — Cancelled publication; Linux CI pack-policy investigation
- At the user's request, cancelled the rerun of Publish run `33673335071` before validation completed and before any package publication or GitHub Release creation. The run is terminal `cancelled`; the immutable tag remains `v17.0.9-alpha.1` at `f736dbae25679b4e34d4d6ae8ff70ad776f10156`.
- Investigated the independent CI PR run `33673325967`. Windows validation passed; Linux validation (primary) passed restore, build, warning ratchet, and all tests, then failed only at `Pack release packages`.
- Root cause is a release-policy mismatch: CI invokes `scripts/release/Pack-Release.ps1 -AllPackages`, whose synthetic release object hard-codes `AllowBreakingChanges = $false`. It therefore validates `Musoq.Plugins` and `Musoq.Schema` 17.0.9-alpha.1 against the 17.0.8-alpha.1 ABI baseline and rejects the intentional alpha changes (`ISchemaColumn.SourceReadType`, `ISchemaColumn.EnumType`, and removed TextInterpreterBase members).
- No production, release-script, workflow, tag, or package changes were made while investigating. The explicit tag path already skips the ABI baseline for alpha; the CI all-packages path needs a separately authorized policy fix before publication is resumed.

## Current Work Unit (2026-09-02) — Publish Core 17.0.9-alpha.1 blocked by NuGet credential
- Squashed the Core-only release work into `f736dbae25679b4e34d4d6ae8ff70ad776f10156`, pushed `feature/runtime_v2_final`, and created/pushed immutable tag `v17.0.9-alpha.1` at that exact commit.
- Tag-triggered Publish run `33673335071` passed validation and packing but failed at `Push packages to NuGet.org` on the first package (`Musoq.Parser`) with HTTP 403: the Trusted Publishing output was empty and the fallback `NUGET_MUSOQ_KEY` was rejected as invalid, expired, or unauthorized.
- No GitHub Release was created and all five exact `17.0.9-alpha.1` NuGet entries are absent. The tag is immutable and must be reused; do not create a replacement tag/version for this credential failure.
- Resume by rotating/updating the `NUGET_MUSOQ_KEY` secret with a valid key authorized for the five Core packages, or configure NuGet Trusted Publishing with the required `NUGET_USER`/publisher relationship, then rerun Publish run `33673335071`. No DataSources, Cloud, CLI, or Website changes are required.

## Current Work Unit (2026-09-02) — Prepare Core 17.0.9-alpha.1 publish
- Prepared the explicit Core-only full-train candidate `v17.0.9-alpha.1` for `Musoq.Parser`, `Musoq.Plugins`, `Musoq.Schema`, `Musoq.Evaluator`, and `Musoq.Converter`; `Musoq.Targets.CSharpClr` remains bundled through Converter.
- Added all five version properties, the changelog entry, curated release notes, and an explicit alpha release policy. Alpha tags intentionally allow breaking datasource ABI changes; preview, beta, and stable tags retain compatibility validation.
- The first pack attempt correctly exposed same-major ApiCompat failures in the intended enum-era Schema surface. No compatibility shims were retained after the user clarified the alpha policy.
- Release-script tests and tag validation passed; restore and the warning-clean Release build passed; the full solution passed 19,755 tests with 3 intentional generated-sample refresh skips and 0 failures.
- Alpha-policy packing produced exactly five `.nupkg` and five `.snupkg` files in `artifacts/release-17.0.9-alpha.1-rerun2`; clean-consumer package smoke passed with 0 warnings/errors. The Cloud-owned development manual audit remained current at 89 chapters and 25/25 checks.
- No commit, push, tag, workflow dispatch, GitHub release, NuGet publication, or separate-repository change was performed. The next phase is user-authorized publication and exact remote/NuGet verification.

## Current Work Unit (2026-09-02) — First-class enum support in Core
- Completed the eight requested local Core waves through `test(enum): qualify first-class enum support`: specification/baseline, parser declarations, portable Schema ABI, binding/discovery, relational semantics/intrinsics, primitive-carrier lowering/output, dynamic-source/predicate contracts, and qualification.
- Enum values remain nominal at compile time and use only typed integral carriers at execution. Final `Row.this[int] : object` boxing remains unchanged; generated-loop and IL checks reject enum parsing, reflection, object reads, descriptor construction, and `box` instructions in enum shape loops.
- Query-local and reachable native enum metadata is portable and fingerprinted; final columns expose numeric carrier types/values plus `EnumTypeDescriptor`, with no retained CLR enum `Type`. Artifact reuse, collectible load-context release, schema drift, fingerprint corruption, typed-output rejection, and source accepted/residual predicate partitions are covered.
- The warning-clean Release build passed. The complete Core solution suite passed 19,755 tests with 3 intentional generated-sample refresh skips and 0 failures. The Q324 enum sample and manifest/coverage guards are current.
- Three complete MediumRun cohorts (48 cases and 1,440 measured case iterations before outlier handling) passed the enum gate. Paired-median ratios were Equality 0.9964x, IN 1.0064x, Flags 0.9695x, Join 0.9989x, Grouping 0.9992x, Distinct 0.9686x, Helpers 0.9752x, and Projection 1.0012x. The largest positive allocation delta was 16 B for an entire 8,192-row operation (0.001953 B/row).
- The separate Musoq.DataSources and Musoq.Cloud repositories were not modified. No push, tag, version change, package publication, or other remote action was performed.
- Next step: the user publishes the exact enum-capable Core prerelease package train. Record those immutable package identities before beginning DataSources alignment and SeparatedValues enum decoding/pushdown; Cloud remains deferred.

## Last Updated
2026-09-02 Core 17.0.9-alpha.1 published; final verification complete

## Current Work Unit (2026-08-30) — Complete specification-to-code-generation sample coverage
- Added the test-owned Q268–Q323 generated sample tranche: 34 core-language samples, 17 binary/text interpretation samples, and 5 TABLE/COUPLE samples. The catalog is split into behavior-family partials; every new catalog file is below the 350-line maintainability limit.
- Added test-only named/default argument, settings-profile, typed TABLE/read-modifier, object-coercion, and deterministic TABLE/COUPLE composition providers. No production code or public API changed.
- Added `specs/generated-code-sample-coverage.json` covering all 488 headings in the three implemented specifications (`musoq-core-language-spec.md`, `musoq-table-couple-spec.md`, and `musoq-binary-text-spec.md`); AI is absent. The ledger includes existing and new snapshots plus explicit non-codegen/diagnostic/host-only/unsupported/future boundaries.
- Added ledger/drift validation, specification execution, and focused generated-shape assertions for join row presence, aggregate/window/set kernels, TABLE metadata/read modifiers, and binary/text routing. Three explicit shape exceptions document unavoidable anti-semi casts or typed materialization in Q272/Q273/Q292/Q321.
- Refreshed all 56 current snapshots and the tracked manifest. Current corpus is exactly 326 files and profiled corpus remains exactly 19 files. Updated the duration guardrail, inventory assertion/test name, and evaluator-performance documentation from 270 to 326.
- Focused generated/ledger/shape/execution suites passed (636 tests, 1 intentional refresh skip); interpretation dump passed (2 tests, 1 skip); core/TABLE/settings/binary/text focused suites passed 272 tests; generated execution suites passed 59 tests plus the named-argument execution test; count/guard/maintainability suites passed 74 tests; full Release solution build passed with 0 warnings/errors; full Release solution suite passed 18,589 tests with 3 intentional refresh skips and 0 failures.
- Re-read `.github/copilot-instructions.md`, ran `git diff --check`, and verified no production-project or profiled-corpus changes. No commit, push, release, or remote change was made.
- Remaining step: final handoff only; preserve the working tree for the user to review and commit manually.

## Current Work Unit (2026-08-26) — Core resolution diagnostic campaign
- Added the reusable resolution-tranche definition, authored expectations, paired near-miss cases, deterministic 150-case saturation and post-fix populations, and fixture-aware contract tests under `src/dotnet/Musoq.Evaluator.Tests/Diagnostics/Campaign/`.
- Generalized campaign state bootstrapping and candidate prefixes while preserving T1 APIs; the fresh ledger is `.codex/diagnostics/state/t3-core-resolution/` and the three corpora are `.codex/diagnostics/corpus/t3-resolution-*.jsonl`.
- Narrow production fixes preserve source/method spans through parser cloning/rewriting, anchor unknown schema/source and ambiguous columns precisely, remove generated evaluator context from unknown-property messages, and make duplicate-alias messages stable/user-facing across Analyze and Compile.
- Resolution campaign state is `succeeded=true`: 320 novel candidates, 3 logical fixes, 22/22 coverage cells covered, 0 findings, no hard stop, and a 320-candidate post-fix streak. Expectations were never derived from observed diagnostics.
- Focused resolution contract/state tests passed 3/3; campaign infrastructure/evaluator semantic/alias/column/analyzer filters passed 147/147; parser alias/recovery/AST filters passed 108/108. Release project build is warning/error clean.
- Remaining gate: full Release solution build and full Release solution test, then final `git diff --check`/worktree audit. No commit, push, release, or publication is authorized.

## Current Work Unit (2026-08-25) — Diagnostic campaign infrastructure
- Implemented the opt-in test-only campaign layer under `src/dotnet/Musoq.Evaluator.Tests/Diagnostics/Campaign/`: authored candidate models/expectations, independent recovery-parser/ValidateSyntax/Analyze/Compile surface runner, structured contract assessment, baseline validation, corpus codec, canonical JSON and length-prefixed SHA-256 fingerprints, structural duplicate index, append-only ledger with terminal-tail recovery and atomic projections, lifecycle/state helpers, run/finding/fix records, and exact DiagnosticAction TextEdit validation.
- Added four hand-authored bootstrap cases: MQ1001 unknown token, MQ3001 unknown column, a valid `@`-inside-string near miss, and a `SELCT` typo with a mechanically validated SELECT quick fix. Expectations are locked from authored root causes before any observation; all bootstrap cases remain `countsTowardCampaign=false`.
- Added the draft T1 state and corpus: `.codex/diagnostics/corpus/bootstrap.jsonl`, four non-campaign bootstrap regressions in `regressions.jsonl`, and `.codex/diagnostics/state/t1-core-lexical/` with the manifest, current candidate `T1-C000001`, 54 required draft base cells, all append-only streams, and artifacts placeholder. Contextual pairwise expansion and candidate generation have not started.
- Focused campaign tests pass 15/15 after adding expectation-hash tamper protection. The combined diagnostic regression filter covering the campaign harness, `DiagnosticContractOracleTests`, `TypoAndUxDiagnosticProbeTests`, and all `ErrorQuality` tests previously passed 354/354. Release project build passes with 0 warnings and 0 errors.
- Next step: perform the final compliance reread and run the last required regression command. Do not generate the large campaign population in this work unit.

## Current Work Unit (2026-08-25) — Diagnostic quality campaign design
- Inspected the Core, TABLE/COUPLE, and Binary/Text specifications; the diagnostic catalog/types/metadata; lexer and parser recovery/enhancement; `QueryAnalyzer`; evaluator binding and Converter compilation paths; all `ErrorQuality_*`, `TypoAndUxDiagnosticProbeTests*`, and `MalformedQueryErrorTests*` partials; and the additional parser/evaluator/converter diagnostic-contract suites discovered during the audit.
- Added `.codex/diagnostics/DIAGNOSTIC_CAMPAIGN.md`. It defines a specification-led valid-baseline/single-mutation/pre-run-expectation campaign, diagnostic/code/producer/phase inventories, mistake taxonomy, locked trigger/near-miss matrix, lifecycle and acceptance contract, minimization/regression/`TextEdit` rules, structural novelty, P0-P3 severity, resumable state, tranches, exact success conditions, and hard stops.
- The first tranche is “Core lexical, tokenization, literal, delimiter, and basic statement-structure diagnostics.” Its base matrix requires trigger and near-miss coverage for 27 codes; after coverage and the final production fix it requires 150 consecutive novel full-contract passes. The hard stops are 500 novel candidates, 30 logical production fixes, 20 consecutive duplicate attempts, more than 10% specification-blocked cells, or a broad compiler redesign; none is success.
- Current readiness finding: the repository has sufficient specifications, structured diagnostics/envelopes/actions, analyzer/compiler surfaces, exact assertion helpers, and deterministic fixtures to begin, but lacks a persistent campaign runner, locked expectation store, fingerprints, pair reducer, complete edit-application validator, and streak accounting. Several tranche-1 descriptors have no direct production emitter, and weak historical probes are explicitly excluded as the oracle.
- No production code or test source was modified. The only task artifacts are the campaign document and this required session handoff.
- Validation passed with SDK 10.0.303: restore succeeded; Release solution build passed with 0 warnings and 0 errors; the full Release suite passed 18,519 tests with 3 intentional generated-sample refresh skips and 0 failures (Schema 480, Parser 1,962, Plugins 4,743, Git 45, CSV 49, Benchmarks 72, Converter 1,047, Evaluator 10,121).
- Next step, only when authorized: implement the local campaign harness/state layer without changing diagnostic behavior, lock the enumerated T1 coverage matrix, and begin baseline/mutation collection. Production remediation remains outside this work unit.

## Current Work Unit (2026-08-25) — Specification-derived negative-query diagnostic matrix
- Completed a 306-case hand-authored matrix derived only from `specs/musoq-core-language-spec.md` and `specs/musoq-table-couple-spec.md`: 257 core cases and 49 TABLE/COUPLE cases.
- Wrote canonical evidence to `TestResults/spec-query-findings.jsonl` and the human report to `TestResults/spec-query-findings.md`. Every JSONL record preserves the original query and exact evaluator response with SHA-256 fields and retains `classification: unclassified`, empty `proposedAction`, and empty `notes`.
- Final outcomes are 297 compile failures, 4 runtime failures, and 5 specification-negative queries accepted and executed. The report calls out the five accepted cases without classifying them.
- Integrity validation passed for all 306 physical JSONL records and 306 Markdown case sections: required fields, unique IDs, allowed specification paths, immutable query/response hashes, failure diagnostic fields, successful execution row counts, UTF-8 without BOM, and classification placeholders all passed.
- The pre-existing generated corpus remained unchanged: `TestResults/query-error-findings-2026-08-24.md` SHA-256 is `809ef9784ac58ac33faa94f7479a6b9a99548c5b42a0562c4bce4fe7dcefa62a`; `TestResults/query-error-probe-runner.csx` SHA-256 is `b58391f3ac904d780160ac07221a4f3c89902eb4af67e21d5f493b571c78da2f`.
- Release solution build passed with 0 warnings and 0 errors. The full Release suite passed 18,510 tests with 3 intentional generated-sample refresh skips and 0 failures: Schema 480, Parser 1,962, Plugins 4,743, Git 45, CSV 49, Benchmarks 72, Converter 1,038, and Evaluator 10,121.
- No production code or test source was modified. Temporary probe sources and build/publish directories were removed after the final evidence run.

## Current Work Unit (2026-08-24) — Corrective stability-aware scalar reuse
- Corrective waves C0–C16 are preserved as focused local commits covering the recomputation baseline, public stability/replayability contracts, shared scalar-reuse model, operator-boundary facts, source projection negotiation, recursive invariants, end-to-end counters, and ten-family qualification.
- C17 is committed as `5e5c20a20` (`feat(evaluator): enable stability-aware scalar reuse`): the umbrella option is default-on and remains independent from LICM/CSE; Q252–Q267 were added; the current corpus is 270 samples; the profiled corpus is 19 samples; all current/profiled samples and the manifest were regenerated; ScalarReuse shape and execution tests were added.
- Stability-aware reuse remains static and branch-free in generated code: direct locals, typed carriers, generated row fields, or existing arrays are used; no runtime cache dictionaries, initialization flags, stability checks, or per-row profitability branches are introduced. Volatile fields/callables, injected context, unknown dynamic access, and unknown replayability remain protected.
- The C17 focused suite passed the new sample shape/execution checks (18/18). The mandatory normal-parallel Release gate passed with 0 warnings/errors: Schema 480, Parser 1,962, Plugins 4,743, Git 45, CSV 49, Benchmarks 70, Converter 1,038, Evaluator 10,121 plus 3 intentional refresh skips (18,507 passed, 0 failed).
- C18 documentation and architecture guardrails are complete in the working tree: provider guidance, replayability, source projection qualification, unsupported boundaries, benchmark thresholds, optimizer classification, and public XML contracts are documented across `docs/`, `.claude/rules/architecture.md`, and module guides.
- C18 focused review passed `git diff --check`; the mandatory normal-parallel Release gate passed with 0 warnings/errors: Schema 480, Parser 1,962, Plugins 4,743, Git 45, CSV 49, Benchmarks 70, Converter 1,038, Evaluator 10,121 plus 3 intentional refresh skips (18,507 passed, 0 failed).
- Post-C18 compliance review found one qualification-harness issue: BenchmarkDotNet rejected the sealed family fixture and the matrix omitted the required no-candidate workload. The corrective working tree makes the fixture inheritable, adds `NoCandidate`, raises the family gate to 120 comparisons (10 families × 4 workloads × 3 fan-outs), and adds a complete synthetic-gate test. The focused benchmark suite passed 12/12 and the corrective mandatory Release gate passed with 0 warnings/errors: Benchmarks 72 and Evaluator 10,121 plus 3 intentional refresh skips; all other solution projects remained green. The corrective commit is the final local change; no push, tag, publication, history rewrite, or external-repository change is authorized.

## Current Work Unit (2026-08-19) — Dynamic wildcard metadata hardening
- Confirmed the dynamic-source regression: pre-binding `WHERE Id` discovery truncated a direct `*`, causing `* EXCLUDE (Other)` to report false MQ3041.
- Wave 1 committed `bf4522474` (`fix(evaluator): preserve wildcard metadata discovery`): direct SELECT-field wildcard extraction now marks complete metadata per source key and leaves aggregate stars unchanged; added a projection-sensitive schema fixture and end-to-end regressions.
- Wave 2 committed `736cae4e8` (`fix(evaluator): harden dynamic star modifier semantics`): added dynamic `LIKE`/`NOT LIKE`/`EXCLUDE`/`REPLACE`/`RENAME`, complete-chain, clause-only binding, diagnostic, and qualified-chain parser coverage.
- Wave 3 committed `57462a232` (`fix(evaluator): isolate wildcard discovery by query scope`): added join, CTE, derived, sibling-CTE, set-branch, correlated, reused-alias, and APPLY ownership coverage.
- Wave 4 committed as `c8bb574ca` (`test(evaluator): lock wildcard metadata boundaries`): added inspection, diagnostic collection, legacy/query-scoped execution, semantic-cache reuse, static-schema and aggregate/pivot/constant negative coverage; documented empty `AllColumns` as the unrestricted metadata convention; fixture records metadata, execution, and query-row contexts.
- Projection-sensitive evaluator coverage passed 37 tests; raw extraction coverage passed 7 tests. Release solution build passed with 0 warnings/errors; the full suite passed 9,913 evaluator tests with 4 intentional refresh skips, plus Schema 470, Parser 1,958, Plugins 4,163, CSV 49, Git 45, Benchmarks 54, and Converter 1,017.
- No changes were pushed. Final compliance-gate rerun of focused wildcard filters, full Release build, full Release suite, `git diff --check`, and commit-boundary verification remains the next step.

2026-08-13 unambiguous, agent-ready diagnostic system Wave 9

## Current Work Unit (2026-08-13) — Generated code sample refresh
- Regenerated the current and profiled generated-code sample refresh utilities through a temporary local runner; refreshed the interpretation samples and manifest as part of the same pass.
- The refresh changed 38 files under `generated-code-samples/current` to include the current runtime-diagnostic wrapper shapes. The 13 profiled samples and `GeneratedCodeSamplesManifest.txt` remained unchanged.
- Generated sample, shape, interpretation, profiled, and manifest validation passed: 538 passed, 4 intentional refresh utilities skipped. `git diff --check` passed and the temporary runner was removed.

## Current Work Unit (2026-08-13) — Unambiguous diagnostic system
- Diagnostic-system baseline was clean at `4d1197536`; the pre-existing parser files `src/dotnet/Musoq.Parser/Parser.Parameters.cs` and `src/dotnet/Musoq.Parser.Tests/ParamsAliasParserTests.cs` remained unchanged and unstaged throughout this work unit.
- Waves 1–6 are preserved and committed: structured diagnostic payloads, descriptor registry, phase barriers, precise parser syntax codes, deterministic symbol resolution, and callable-resolution classification.
- Wave 7 is committed as `1ef18efde` (`fix(evaluator): diagnose constant and type failures early`): added `MQ3090_UnsupportedCastTarget`, `MQ3091_InvalidConstantCast`, `MQ3092_AggregateInGroupBy`, `MQ3093_OrderByOrdinalUnsupported`, `MQ3094_InvalidConstantRegex`, `MQ3095_ScalarSubqueryCardinality`, `MQ3096_UnsupportedVariableKeyAccess`, `MQ3097_UnsupportedAggregateProjection`, and `MQ4016_UnsupportedSchemaConstruction`; added strict constant-cast validation, bind-time constant-regex validation, authored-tree scalar-cardinality analysis, precise variable-key/aggregate/group/order/schema classifications, and dependent-diagnostic suppression.
- Wave 7 focused coverage passed: Evaluator precision diagnostics 8/8, Converter propagation 1/1, and column-resolution regressions 25/25.
- Wave 7 affected Release projects passed: Parser 1,951/1,951; Evaluator 9,785 passed with 4 pre-existing skipped refresh utilities; Converter 968/968; Schema 457/457.
- Wave 7 final Release solution build passed with 0 warnings and 0 errors. The complete solution suite passed 17,447 tests, skipped 4 pre-existing refresh utilities, and had 0 failures. Diff checks passed.
- Wave 8 is committed as `f033eb76e` (`test(converter): verify advisory warning propagation`) with an additional exact MQ5015 contract comparison across `QueryAnalyzer`, synchronous/options/asynchronous compilation, inspection, and `CreateForAnalyze`, including runtime compatibility. Focused propagation coverage passed 8/8. The final Release build passed with 0 warnings and 0 errors; the complete solution suite passed 17,448 tests, skipped 4 pre-existing refresh utilities, and had 0 failures: Schema 457, Parser 1,951, Plugins 4,163, CSV 43, Git 45, Benchmarks 35, Converter 969, Evaluator 9,785. The first 120-second test invocation timed out without results; the unchanged rerun with a 300-second limit completed in 140.3 seconds and passed. Diff checks passed.
- Wave 9 implementation is ready to commit as `fix(converter): preserve code generation diagnostic origins`: MQ8001 now explicitly uses CodeGeneration/GeneratedSource, keeps target-native codes in structured `targetCode` arguments, retains generated offsets and line/column data without SQL clamping, and uses stable UTF-8 encoded `.g.cs` paths for Roslyn trees and interpreter trees. Focused reporter coverage passed 2/2 and the combined converter propagation/reporter coverage passed 10/10. The final Release build passed with 0 warnings and 0 errors. The complete solution suite passed 17,449 tests, skipped 4 pre-existing refresh utilities, and had 0 failures: Schema 457, Parser 1,951, Plugins 4,163, CSV 43, Git 45, Benchmarks 35, Converter 970, Evaluator 9,785. The first full run exposed 208 `CS8055` failures caused by missing source encoding; explicit UTF-8 encoding fixed them and the unchanged rerun passed in 123.8 seconds. Diff checks passed; staged checks and the Wave 9 commit remain required.

## Current Work Unit (2026-08-11)
- Advisory warning-suite baseline: clean at `77f578646`; Release build passed with 0 warnings and 0 errors; the complete solution suite passed 17,213 tests, skipped 4 pre-existing refresh utilities, and had 0 failures.
- Wave 1 implementation is ready to commit as `feat(evaluator): warn on suspicious pattern literals`: added `MQ5015_SuspiciousRegexEscape` and `MQ5016_GlobWildcardInLike`, shared post-binding advisory coordination for `QueryAnalyzer` and compilation, literal-origin resolution through constant script-variable chains, and focused regex/LIKE coverage.
- Wave 1 final Release build passed with 0 warnings and 0 errors. The complete solution suite passed 17,217 tests, skipped 4 pre-existing refresh utilities, and had 0 failures. Focused pattern coverage passed 4/4; maintainability guardrails passed 66/66; `git diff --check` passed.
- Wave 2 implementation is ready to commit as `feat(evaluator): warn on null and impossible predicates`: added `MQ5017_NullComparison`, shared predicate-context analysis, constant-let null resolution, conservative direct-column tautology/contradiction proofs, and moved generic condition warning ownership out of logical optimization.
- Wave 2 final Release build passed with 0 warnings and 0 errors. Parser passed 1,858/1,858; Evaluator passed 9,674 with 4 pre-existing skipped refresh utilities; the complete solution suite passed 17,222 tests, skipped 4, and had 0 failures. Focused predicate coverage passed 5/5; maintainability guardrails passed 66/66; `git diff --check` passed.
- Wave 3 implementation is ready to commit as `feat(evaluator): warn on outer join null traps`: added `MQ5018_AmbiguousOuterJoinNullCheck` and `MQ5019_NullRejectingOuterJoinFilter`, conservative optional-alias extraction across outer/full/right/ASOF-left joins and OUTER APPLY, original source-column nullability checks, WHERE-only three-valued missing-row predicate proofs, presence-guard suppression, and no warning ownership in JOIN ON.
- Wave 3 final Release build passed with 0 warnings and 0 errors. Parser passed 1,858/1,858; Evaluator passed 9,686 with 4 pre-existing skipped refresh utilities; the complete solution suite passed 17,234 tests, skipped 4, and had 0 failures. Focused outer-join coverage passed 12/12; maintainability guardrails passed 66/66; `git diff --check` passed.
- Wave 4 implementation is ready to commit as `feat(evaluator): report unreachable query branches`: activated `MQ5008_UnreachableCode` for statically false/NULL and duplicate deterministic CASE conditions, unreachable true tails, constant script conditions, and statically non-null coalesce fallbacks; advisory analysis inspects the normalized source tree before coalesce pruning and preserves runtime behavior.
- Wave 4 final Release build passed with 0 warnings and 0 errors. Parser passed 1,858/1,858; Evaluator passed 9,693 with 4 pre-existing skipped refresh utilities; Converter passed 947/947; the complete solution suite passed 17,241 tests, skipped 4, and had 0 failures. Focused unreachable-branch coverage passed 7/7; `git diff --check` passed.
- Wave 5 implementation is ready to commit as `feat(evaluator): warn on unused query declarations`: added `MQ5022_UnusedCte` and `MQ5023_UnusedScriptVariable`, reused `CteDependencyGraphBuilder` reachability, skipped dead CTE bodies when computing variable roots, followed transitive let dependencies, retained recursive self-reference handling, and reported source declaration spans.
- Wave 5 final Release build passed with 0 warnings and 0 errors. Parser passed 1,858/1,858; Evaluator passed 9,703 with 4 pre-existing skipped refresh utilities; Converter passed 947/947; the complete solution suite passed 17,251 tests, skipped 4, and had 0 failures. Focused declaration coverage passed 10/10; `git diff --check` passed.

- Wave 6 implementation is ready to commit as feat(evaluator): warn on misleading ordering and slicing: added MQ5020_SetOperationOrderByScope and MQ5021_UnorderedSkip, shared source-tree analysis for set-operation order scope and positive unordered skips, branch-local TAKE/positive-SKIP suppression, SKIP 0 handling, CTE/derived outer-order suppression, and retained dormant MQ5009_OrderByAliasBehavior behavior.
- Wave 6 final Release build passed with 0 warnings and 0 errors. Parser passed 1,858/1,858; Evaluator passed 9,714 with 4 pre-existing skipped refresh utilities; the complete solution suite passed 17,262 tests, skipped 4, and had 0 failures. Focused ordering/slicing coverage passed 11/11; Schema 457/457, Plugins 4,163/4,163, CSV 43/43, Git 45/45, Benchmarks 35/35, and Converter 947/947 also passed; diff checks passed.
- Wave 7 implementation is ready to commit as feat(evaluator): expand context-aware string warnings: activated conservative MQ5003 ambiguous numeric-date detection for implicit DateTime/DateTimeOffset conversion, preserved implicit-conversion source spans, extended MQ5014 to direct path-sensitive string columns and constant-let origins, excluded RLIKE and rooted-warning duplicates, and split warning metadata to preserve the maintainability family budget.
- Wave 7 final Release build passed with 0 warnings and 0 errors. Parser passed 1,858/1,858; Evaluator passed 9,735 with 4 pre-existing skipped refresh utilities; Converter passed 947/947; the complete solution suite passed 17,283 tests, skipped 4, and had 0 failures. Focused contextual-string coverage passed 21/21; Schema 457/457, Plugins 4,163/4,163, CSV 43/43, Git 45/45, and Benchmarks 35/35 also passed; maintainability guardrail passed; diff checks passed.
- Wave 8 test-only propagation coverage is ready to commit as test(converter): verify advisory warning propagation: added analyzer-versus-compilation contract coverage for MQ5003 and MQ5008/MQ5010/MQ5011/MQ5014-MQ5023, options and async compilation, inspection, CreateForAnalyze, syntax-only versus binding warnings, warning-only runtime compatibility, concurrent cache replay/isolation, failed-cache recovery, and deterministic mixed-warning ordering. Focused propagation coverage passed 7/7; Parser passed 1,858/1,858; Evaluator passed 9,735 with 4 pre-existing skipped refresh utilities; Converter passed 954/954; the complete solution suite passed 17,290 tests, skipped 4, and had 0 failures. Release build passed with 0 warnings and 0 errors; diff checks passed.

- Wave 9 documentation and contract coverage is ready to commit as `docs(language): document advisory query warnings`: documented the active warning catalog and phases, diagnostic-aware versus syntax-only surfaces, regex/LIKE/NULL/outer-join/SKIP examples, CASE/CTE/date/path guidance, and dormant `MQ5009` compatibility behavior; added parser metadata/formatter checks and specification-exploration compilation/runtime tests.
- Wave 9 focused coverage passed: parser documentation tests 3/3; evaluator specification advisory tests 2/2; existing dormant MQ5009 alias regression 1/1.
- Wave 9 affected-project gates passed: Parser 1,861/1,861; Evaluator 9,737 passed with 4 pre-existing skipped refresh utilities; Converter 954/954.
- Wave 9 Release solution build passed with 0 warnings and 0 errors. The complete solution suite passed 17,295 tests, skipped 4 pre-existing refresh utilities, and had 0 failures: Schema 457, Parser 1,861, Plugins 4,163, CSV 43, Git 45, Benchmarks 35, Converter 954, Evaluator 9,737.
- Wave 9 `git diff --check` passed before staging; staged diff checks and the final full Release build/test gate remain required before handoff.
- Wave 10 is ready to commit as `fix(schema): classify datasource execution failures`: added `MQ7010_DataSourceOpenFailed`, `MQ7011_DataSourceReadFailed`, and `MQ7012_DataSourceCleanupFailed`; wrapped provider, source-open, synchronous/deferred enumeration, asynchronous enumeration, profiled execution, and cleanup boundaries; preserved cancellation and original inner exceptions; added safe schema/source/alias/context lifecycle facts; and retained structured schema/method resolution reasons.
- Wave 10 focused lifecycle coverage passed 24/24, including direct open/read/cleanup/cancellation/async cases, compiled-query open/read/cleanup failures, safe envelopes, source context, and recursive enumeration compatibility. The first full Evaluator run passed 9,792 of 9,798 outcomes with 4 skips and exposed only the intended maintainability budgets; after explicit budget updates, the complete Evaluator suite passed 9,794 with 4 pre-existing skipped refresh utilities and 0 failures.
- Wave 10 final Release build passed with 0 warnings and 0 errors. The complete solution suite passed 17,458 tests, skipped 4 pre-existing refresh utilities, and had 0 failures: Schema 457, Parser 1,951, Plugins 4,163, CSV 43, Git 45, Benchmarks 35, Converter 970, Evaluator 9,794. `git diff --check` passed; staged checks and the Wave 10 commit remain required.
- Wave 11 is ready to commit as `fix(evaluator): classify runtime expression failures`: added MQ7013-MQ7019 runtime expression diagnostics, source-origin propagation from parser/IR into generated execution wrappers, typed runtime-boundary classification for callable, cast, regex, scalar-subquery, dynamic-member, arithmetic, timeout, and cancellation paths, sanitized `QueryExecutionException` envelopes with explicit verbose formatting, and lifecycle/materialization coverage without changing successful query behavior.
- Wave 11 focused runtime-diagnostic coverage passed 337/337; the complete Evaluator suite passed 9,799 with 4 pre-existing skipped refresh utilities and 0 failures. The final Release solution build passed with 0 warnings and 0 errors. The complete solution suite passed 17,463 tests, skipped 4 pre-existing refresh utilities, and had 0 failures: Schema 457, Parser 1,951, Plugins 4,163, CSV 43, Git 45, Benchmarks 35, Converter 970, Evaluator 9,799. The first final suite run exposed only a four-line existing test-fixture budget adjustment after generated-code expectation updates; the unchanged rerun passed. `git diff --check` and staged checks remain required before commit.
- Wave 12 is ready to commit as `test(converter): verify advisory warning propagation`: added the all-diagnostics `BuildResult.ToAllEnvelopes()` surface while preserving error-only `ToEnvelopes()`, and strengthened propagation tests across analysis, options, async, inspection, analyze creation, cache replay, concurrency, warning-only execution, source ordering, and structured payload fields.
- Wave 12 focused propagation coverage passed 9/9. The final Release solution build passed with 0 warnings and 0 errors. The complete solution suite passed 17,464 tests, skipped 4 pre-existing refresh utilities, and had 0 failures: Schema 457, Parser 1,951, Plugins 4,163, CSV 43, Git 45, Benchmarks 35, Converter 971, Evaluator 9,799. `git diff --check` and staged checks remain required before commit.
- Wave 13 is ready to commit as `refactor(parser): remove legacy diagnostic ambiguity`: removed the selected dormant, stale, and overlapping v17 enum members and descriptors without reusing their numeric values; migrated method-resolution exceptions to MQ3086-MQ3089, construction defaults to MQ4016, datasource tests to MQ7010/MQ7011, and generic internal envelopes to MQ9001; deleted the unused set-operator exception path; and added removed-value/active-descriptor guard coverage.
- Wave 13 focused taxonomy coverage passed: Parser 1,950/1,950 and Evaluator 9,790 with 4 pre-existing skipped refresh utilities. The final Release solution build passed with 0 warnings and 0 errors. The complete solution suite passed 17,454 tests, skipped 4 pre-existing refresh utilities, and had 0 failures: Schema 457, Parser 1,950, Plugins 4,163, CSV 43, Git 45, Benchmarks 35, Converter 971, Evaluator 9,790. `git diff --check` and staged checks remain required before commit.

## Current Work Unit (2026-08-12) — Context-aware alias grammar
- Alias-refactor preflight was clean at `4c7bab872`; the existing warning-suite baseline had a Release build with 0 warnings and 0 errors and a complete solution suite of 17,295 passed, 4 skipped, and 0 failed.
- Wave 1 committed `f312994c8` (`refactor(parser): centralize alias grammar`) with shared context-aware alias parsing, boundary/token-role handling, and MQ2022 malformed-alias coverage.
- Wave 2 committed `1036e7579` (`refactor(parser): model source alias provenance`) with source-kind facts, source-end insertion spans, natural CTE/in-memory aliases, and nested alias-scope coverage.
- Wave 3 committed `e1c83e003` (`fix(parser): require stable aliases in multi-source queries`) with public Parse/Error `MQ2035_MissingRequiredAlias`, stable alias enforcement for JOIN/APPLY families, derived/VALUES requirements, and precise diagnostic metadata.
- Wave 4 committed `8ed0caba4` (`fix(parser): harden source clause recovery boundaries`) with boundary/recovery matrices, MQ2007 join-condition precedence, semicolon/CRLF recovery, and one-primary-diagnostic assertions.
- Wave 5 committed `42c21f966` (`test(converter): verify required alias diagnostics end to end`) with analyzer, synchronous/asynchronous compilation, inspection, cache, malformed short-circuit, valid JOIN/APPLY execution, and corrected-alias runtime coverage.
- Wave 6 documentation and specification tests add the stable-source EBNF, JOIN/APPLY/derived/VALUES/PIVOT/UNPIVOT contracts, reserved-word alias examples, the reported `CROSS APPLY ... TAKE` case, and specification-exploration execution coverage.
- Wave 6 focused specification coverage passed 5/5. Affected Release projects passed: Parser 1,915, Converter 962, and Evaluator 9,742 with 4 pre-existing skipped refresh utilities. The full Release solution build passed with 0 warnings and 0 errors. The complete solution suite passed 17,362 tests, skipped 4, and had 0 failures: Schema 457, Parser 1,915, Plugins 4,163, CSV 43, Git 45, Benchmarks 35, Converter 962, Evaluator 9,742.
- Wave 6 `git diff --check` and staged checks remain required before the final documentation commit. No changes were pushed or published.

## Current Work Unit (2026-08-08)
- Preflight baseline: existing branch state was clean at `2deacd890`; full Release solution green with 9,593 Evaluator tests and 4 pre-existing skipped refresh utilities.
- Wave 1 committed `fe3f542c6` (`test(evaluator): establish count-distinct behavioral matrix`) with grouped nullable/decimal/two-key/all-null/empty-input oracle coverage and the zero-key empty aggregate fix; generated snapshots and maintainability budgets were refreshed.
- Wave 2 committed `b1014d15f` (`test(plugins): cover count-distinct overload and merge matrix`) with all 15 typed CountDistinct kernels, Set/Get/merge/empty-state coverage, evaluator overload queries, and descriptor guardrails.
- Wave 3 committed `6cd80d98d` (`test(evaluator): verify count-distinct parallel and streaming execution`) with generated parallel merge shape assertions, duplicate/null partitioned output-oracle coverage, and streaming chunk merge coverage.
- Wave 4 adds end-to-end composition coverage for qualified and case-insensitive syntax, WHERE/FILTER/HAVING/ORDER BY, multiple aggregates, CTEs, joins, pivots, and the recursive-member MQ3075 negative contract; stale unsupported guidance was removed.
- Final Release build/test gate: zero build warnings/errors; Schema 457, Parser 1,754, Plugins 4,163, CSV 43, Git 45, Benchmarks 35, Converter 918, Evaluator 9,612 passed plus 4 pre-existing skips (9,616 total Evaluator outcomes), 0 failures.

## Current Work Unit
- Reproduced and fixed the core-only `SELECT *` through CTE plus `EXCEPT` regression using the existing `PositionalRowsSchemaProvider`.
- Wave 1 committed `50291097c` (`fix(evaluator): preserve indexed access across row carriers`) by preserving complete source columns across materialized CTE boundaries while retaining safe terminal fusion pruning.
- Wave 2 committed `cbed387f1` (`refactor(evaluator): centralize row carrier rebinding`) with metadata-preserving Evaluator-owned `FieldBindingRebinder` use across typed and untyped materialized transitions.
- Wave 3 committed `0d5a64ba5` (`test(evaluator): harden positional row binding coverage`) with set-operation, CTE, sidecar, projection/filter/order/distinct/join, nullable/permuted/nested/opaque/repeated-compilation coverage, syntax-aware generated-code assertions, and the target-neutral `ExecutionBindingInvariantValidator`.
- Wave 4 committed `418c9c63f` (`chore(release): prepare 17.0.4-alpha.4`) with only `Musoq.Evaluator` version metadata, changelog, and package-specific release notes.
- The branch is five commits ahead of upstream, including the pre-existing documentation checkpoint; it is ready for authorized soft-reset flattening and publication.

## Current Status
- Build status: complete Release solution build passes with zero warnings and zero errors.
- Test status: every wave gate passed; the latest unfiltered solution suite passed 9,552 tests, skipped 4 pre-existing snapshot-refresh utilities, and had 0 failures.
- Release status: release scripts, `evaluator/v17.0.4-alpha.4` validation, clean Evaluator-only pack, and selected-package consumer smoke test passed locally.
- Next step: flatten the five unpushed commits into one release commit, revalidate on the flattened SHA, push the branch, run the exact-SHA publish dry run, then tag and verify GitHub assets and NuGet indexing.

## Current Work Unit (2026-08-13) — Unambiguous, agent-ready diagnostic system
- Wave 14 committed as `564dc9233` (`test(evaluator): lock exact diagnostic contracts`): removed alternate-code assertions, added the shared exact diagnostic oracle and independent-root/cascade guardrails, covered warning-only and malformed-query contracts, and completed `MQ5024_NullSensitiveNotIn` plus `MQ5025_ImpossibleImplicitConversion` coverage.
- Wave 14 final Release build passed with 0 warnings and 0 errors. The complete solution suite passed 17,463 tests, skipped 4 pre-existing refresh utilities, and had 0 failures: Schema 457, Parser 1,950, Plugins 4,163, CSV 43, Git 45, Benchmarks 35, Converter 971, Evaluator 9,799.
- Wave 15 documentation/catalog work is complete: replaced the exception-oriented specification section with a code-first diagnostic contract, documented phases/source domains/root-cause classification/advisories/runtime and migration guidance, added README examples for the active warnings, committed the descriptor-backed `specs/diagnostic-catalog.json`, and added catalog freshness plus specification-exploration coverage.
- Wave 15 focused coverage passed: Parser documentation/catalog tests 4/4, core specification exploration 98/98, and the explicit ORDER BY alias compatibility regression passed.
- Wave 15 final Release build passed with 0 warnings and 0 errors. The complete solution suite passed 17,466 tests, skipped 4 pre-existing refresh utilities, and had 0 failures: Schema 457, Parser 1,952, Plugins 4,163, CSV 43, Git 45, Benchmarks 35, Converter 971, Evaluator 9,800. An initial 120-second invocation timed out without a test result, and an intermediate run correctly caught a maintainability guard violation in the new test; after switching to the repository row-matrix assertion helper, the required final rerun completed green.
- No changes were pushed or published. Protected user-owned parser files remained byte-for-byte unchanged and unstaged.

## Previous Session
2026-06-27 runtime-v2 standalone documentation audit and final fixes

## Completed Tasks
- Reworked `MusoqAutonomousPluginDevelopment.md` into a standalone runtime-v2 migration guide for autonomous plugin creation outside the Musoq source repository.
- Reworked `MusoqPluginsDevelopment.md` into a detailed runtime-v2 plugin tutorial with typed rows, dictionary rows, source planning, runtime settings, `TABLE` / `COUPLE`, read modifiers, diagnostics, tests, and migration guidance.
- Strengthened both XML documentation sections to state that generated XML is a critical static discovery contract that lets Musoq tooling list schemas, source methods, tables, and columns without loading the plugin DLL.
- Restored standalone discovery and packaging details from the proven staged docs:
  - `[assembly: PluginSchemas("weather")]` in `Properties/AssemblyInfo.cs`.
  - `<EnableDynamicLoading>true</EnableDynamicLoading>`, package metadata, and `_ResolveCopyLocalNuGetPackageXmls`.
  - Host-provided Musoq assemblies excluded from plugin runtime output.
  - Two-level datasource package with `EntryPoint.txt`, `Platform.txt`, `Architecture.txt`, `LibraryName.txt`, `Version.txt`, and inner `Plugin.zip`.
  - `musoq datasource import` as the default install command, with `musoq plugin install` noted for hosts that expose it.
- Added runtime-v2 `AddTable<T>()` / `GetRawConstructors(SourceMetadataContext)` guidance so `desc #schema` and static catalogs work without runtime-v1 APIs.
- Added discovery, XML, `desc`, and package smoke test snippets to both guides.
- Performed a follow-up runtime-v2 correctness audit after the user asked to make the refreshed docs as close as possible to the old proven docs while targeting runtime-v2.
- Fixed additional audit findings:
  - Kept `COUPLE` examples in the requested no-`#` schema-method form, and verified no `couple #...` examples remain.
  - Added missing `Musoq.Evaluator` import to the tutorial query helper.
  - Made the autonomous predicate planner conservative so unsupported `AND` terms cannot be dropped.
  - Tightened source planning examples so `skip` and `take` are accepted only when predicate and order work before the slice are also accepted; the tutorial projection-only example now leaves slicing residual.
  - Changed dynamic dictionary metadata examples to use `typeof(IReadOnlyDictionary<string, object>)` while keeping row values nullable, avoiding warning-prone nullable annotations inside `typeof(...)`.
  - Added missing standalone imports/namespace notes for `ColumnReadModifiers`, `SourceContractDiagnostic`, and `CultureInfo` snippets.
  - Added script-location assumptions for package build scripts.
  - Added TABLE/COUPLE statement ordering guidance.
  - Added TABLE keyword-to-runtime-type mapping from the spec.
  - Replaced malformed stale-audit command text with pasteable PowerShell single-quoted commands.
  - Aligned early package-reference snippets with host-provided Musoq runtime assembly exclusion.
- Preserved the staged documents' high-level autonomous/tutorial structure while replacing active runtime-v1 implementation guidance with runtime-v2 contracts.
- Re-read `.github/copilot-instructions.md` after edits and verified the changed docs follow the requested architecture and validation guidance.

## Current Status
- Build status: not applicable; documentation-only changes.
- Test status:
  - `dotnet test src/dotnet/Musoq.Schema.Tests --configuration Release --no-build --nologo --verbosity quiet --logger "console;verbosity=minimal"` passed: 457/457.
  - `dotnet test src/dotnet/Musoq.Converter.Tests --configuration Release --no-build --nologo --verbosity quiet --logger "console;verbosity=minimal"` passed: 650/650.
- Documentation checks:
  - `git diff --check -- MusoqPluginsDevelopment.md MusoqAutonomousPluginDevelopment.md` passed.
  - Stale runtime-v1 API audit only reports expected migration/troubleshooting references.
  - Extra guard audit found no `couple #`, no stale outer package metadata names, no old self-closing Musoq package references, and no `typeof(IReadOnlyDictionary<string, object?>)` metadata examples.
  - Non-ASCII scan found no matches in the two refreshed markdown files.
- Known issues: none.

## Next Steps
- Review the refreshed docs for preferred wording and any host-specific plugin packaging conventions that should be added.
- Stage the refreshed documentation and session summary when ready.

## Context Notes
- The docs intentionally target runtime-v2 only and do not preserve runtime-v1 compatibility.
- The current known package train documented in examples is `17.0.0-alpha.1`; the docs tell standalone agents to resolve package versions from the target host/feed rather than treating that version as permanent.
- Remaining references to retired APIs such as `RuntimeContext`, `QuerySourceInfo`, `QueryHints`, `IObjectResolver`, `EntityResolver`, `BlockingCollection`, and `WhereNodeHelper` are confined to explicit migration and troubleshooting sections.

## Current Work Unit (2026-08-14) — Combined plan implementation
- Wave 1 of the combined analyzer cleanup is complete: collection-expression call sites were made explicit in the CSV example, plugin tests, Evaluator, and target analysis.
- Focused Plugins, Evaluator, Converter, and CSV tests passed; the Release solution build passed with 0 warnings and 0 errors; the complete solution suite passed 17,466 tests with 4 pre-existing skipped refresh utilities; `git diff --check` passed.
- Next step: complete the remaining analyzer cleanup waves before publishing Parser contracts and removing the Parser production friend edges.
- Wave 2 is complete: Roslyn argument and initializer list construction now uses explicit `ArgumentSyntax[]` or `ExpressionSyntax[]` arrays in the target renderer.
- Target build, Converter/generated-code focused tests, and the complete Release solution gate passed; the solution suite remained at 17,466 passed with 4 pre-existing skipped refresh utilities.
- Wave 3 is complete: all unbound generic `nameof` operands are closed with constraint-valid concrete types across CSharpClr, Evaluator, and affected tests.
- Renderer/IR focused tests and the complete Release solution gate passed; the solution suite remained at 17,466 passed with 4 pre-existing skipped refresh utilities.
- Wave 4 is complete: parser and diagnostic taxonomy membership checks now use `Array.IndexOf` instead of analyzer-invalid `Contains` calls on arrays.
- Parser-focused tests passed; the complete Release solution build and suite passed with 17,466 tests passed, 4 pre-existing skipped refresh utilities, and 0 failures.
- Wave 5 is complete: the parser now publishes `StringEscapeRisk` and `StringEscapeRiskDetector` as the shared escape-analysis contract, while Evaluator diagnostics consume the public contract; direct parser coverage verifies spans, rooted-path facts, and ignored escapes.
- Wave 5 focused tests passed (Parser detector 3/3 and Evaluator advisory coverage); the complete Release solution build passed with 0 warnings and the suite passed 17,469 tests with 4 pre-existing skipped refresh utilities and 0 failures.
- Wave 6 is complete: `Musoq.Parser.Traversal.AstWalker` is now a documented public depth-first traversal contract while `AstChildren` and `AstChild` remain internal; Converter schema traversal continues to use the shared walker.
- AST traversal and Converter schema traversal focused tests passed (4 and 3 respectively); the complete Release solution build passed with 0 warnings and the suite passed 17,469 tests with 4 pre-existing skipped refresh utilities and 0 failures.
- Wave 7 is complete: `SyntaxDiagnosticFactory` is a documented public facade over the internal syntax enhancer, and Converter's multi-statement diagnostic path now uses that facade; direct tests verify codes, locations, context, suggestions, explanations, docs, and related lexer information.
- Parser diagnostic and Converter advisory focused tests passed; the complete Release solution build passed with 0 warnings and the suite passed 17,471 tests with 4 pre-existing skipped refresh utilities and 0 failures.
- Wave 8 is complete: `Musoq.Parser.csproj` now retains only the `Musoq.Parser.Tests` friend declaration; production Parser friend edges were removed, and dependency-ordered Parser, Converter, and Evaluator builds pass against the public contracts.
- Parser, Converter, Evaluator advisory, and full Release solution gates passed; the complete suite passed 17,471 tests with 4 pre-existing skipped refresh utilities and 0 failures. Source search confirms no production Parser friend entries remain.
- Runtime Wave 1 is complete: unexpected compiled, typed, deferred-materialization, and row-enumeration failures now become safe `MQ9002_InternalExecutionError` envelopes with correlation IDs; inner details remain available only through verbose diagnostics, cancellation is rethrown unchanged, and existing diagnostic/data-source/parameter exceptions remain specialized.
- Wave 9 focused runtime, Converter API, typed execution, deferred-table, and exception tests passed; the complete Release solution build passed with 0 warnings and the suite passed 17,471 tests with 4 pre-existing skipped refresh utilities and 0 failures.
- Runtime Wave 2 is complete: C# execution rendering now emits direct method, cached-method, dynamic-member, strict-cast, scalar-subquery, arithmetic, unary, and pattern expressions without `RuntimeExpressionBoundary` or `RuntimeExpressionOrigin` calls. Renderer and inspection expectations were updated, while snapshot normalization remains temporarily for the dedicated refresh wave.
- Renderer, strict-cast, inspection, transition-runtime, and complete Release solution gates passed; the suite passed 17,471 tests with 4 pre-existing skipped refresh utilities and 0 failures.
- Runtime Wave 3 is complete: the exact `MQ7013`-`MQ7019` expression-diagnostic contract, runtime boundary/origin/exception types, source-span plumbing, and renderer diagnostic support were removed. Regex, scalar-cardinality, cast, dynamic, callable, and arithmetic failures now propagate to the generic execution boundary; the parser catalogs and core specification retain only `MQ7010`-`MQ7012` in this runtime range. The transition-only snapshot normalizer remains for the dedicated refresh wave.
- Runtime Wave 3 focused coverage passed; the complete Release build passed with 0 warnings and the solution suite passed 17,471 tests with 4 pre-existing skipped refresh utilities and 0 failures.
- Runtime Wave 4 is complete: the transition snapshot normalizer and call sites are gone, 38 modified current generated samples were regenerated from the direct renderer, profiled/interpretation artifacts and the manifest were refreshed through the existing utilities, and a corpus guard now rejects retired runtime-diagnostic contracts and wrapper patterns.
- Generated snapshot, shape, profiled, interpretation, manifest, and corpus-guard coverage passed: 539 tests passed with 4 intentionally skipped refresh utilities. The Wave 4 full Release gate follows before commit.

## Current Work Unit (2026-08-17) — Query-scoped CLR rows
- Wave 1 baseline is implemented: `Musoq.Benchmarks` now references the core CSV example and contains `QueryScopedSourceMaterializationBenchmark` plus a committed legacy baseline report.
- Clean baseline run used .NET SDK 10.0.303/.NET 10.0.11 on Windows 11 with an Intel Core Ultra 9 285K. Full-scan legacy CSV allocation increased from 709.77 KB at 2 fields to 9653.21 KB at 64 fields; measured means were 479.2 us to 5.1545 ms.
- Wave 1 Release build passed with 0 warnings and 0 errors. The complete solution suite passed 17,471 tests, skipped 4 pre-existing generated-sample refresh utilities, and had 0 failures. `git diff --check` passed.
- Next step: commit Wave 1, then add the optional `Musoq.Schema` query-scoped reader/materializer contracts while preserving legacy `GetRowSource<T>` behavior.
- Wave 2 is complete: `Musoq.Schema` now exposes optional query-scoped transfer capabilities, immutable `QueryRowShape`/`QueryRowField` metadata with deterministic fingerprints, a static generic materializer contract, and an opt-in schema source interface. Existing `ISchema` and `GetRowSource<T>` implementations are unchanged.
- Wave 2 focused contract coverage passed 5/5. The final Release solution build passed with 0 warnings and 0 errors; the complete suite passed 17,476 tests, skipped 4 pre-existing generated-sample refresh utilities, and had 0 failures. `git diff --check` passed.
- Wave 3 is complete: source discovery now preserves immutable `SourceDescriptor` capability metadata, `SourceTransferPlanner` intersects advertised source support, target support, exact metadata, and conservative carrier policy, and selected query-scoped strategies flow into physical schema scans. Unsupported, unresolved, object-shaped, duplicate, invalid, pointer/byref-like, and otherwise unusable shapes produce explicit legacy fallback decisions.
- Wave 3 focused planner/physical propagation coverage passed 6/6. The final Release solution build passed with 0 warnings and 0 errors; the complete suite passed 17,476 tests, skipped 4 pre-existing generated-sample refresh utilities, and had 0 failures. The first 120-second suite invocation timed out without a result; the unchanged normal-parallel rerun with a 300-second allowance completed in 108.2 seconds and passed. `git diff --check` passed.
- Wave 4 is complete: Execution IR/source bindings now carry portable query-row transfer metadata (carrier, dense slot/source ordinal mapping, exact portable field types, nullability, read modifiers, and shape fingerprint); physical and execution printers include selected fingerprints for identity separation; rewriters preserve the metadata; validators reject malformed transfer bindings; and target analysis advertises a separate typed `query-row-source-access-v1` host import plus target requirement/feature/readiness compatibility.
- Wave 4 focused transfer/ABI coverage passed 4/4. The final Release solution build passed with 0 warnings and 0 errors; the complete suite passed 17,476 tests, skipped 4 pre-existing generated-sample refresh utilities, and had 0 failures. An initial full run caught and fixed the architecture guard requiring typed ABI import details; the unchanged normal-parallel rerun completed in 127.2 seconds and passed. `git diff --check` passed.
- Next step: render query-scoped carrier/materializer code and call the optional generic schema API while retaining legacy source rendering unchanged.
- Wave 5 is complete: the C# renderer emits deterministic private readonly-struct or sealed-class query-row carriers, static generic materializers using concrete-reader `Read<T>(slot)` calls, immutable shape metadata, and the optional `GetQueryScopedRowSource<TRow,TMaterializer>` path. Legacy `GetRowSource<T>` rendering remains unchanged; lifecycle wrapping now preserves and forwards the optional capability. Generated-code and compile/run coverage verifies typed materialization without the legacy object-row boundary, interface field-reader instances, or delegate invocation.
- Wave 5 focused renderer coverage passed 2/2. The final Release solution build passed with 0 warnings and 0 errors. The complete normal-parallel solution suite passed 17,476 tests, skipped 4 pre-existing generated-sample refresh utilities, and had 0 failures: Schema 462, Parser 1,957, Plugins 4,163, CSV 43, Git 45, Benchmarks 35, Converter 973, Evaluator 9,810. The initial full run exposed only the existing ExecutionShapeResolver family budget after the new source-shape helper; moving that helper to a dedicated factory restored the unchanged budget, and the rerun passed in 102.9 seconds. `git diff --check` passed.
- Next step: commit Wave 5, then add the cross-format hardening matrix and test-only ordinal/property/path ref-struct readers for Wave 6.
- Wave 6 is complete: test-only dynamic readers now cover ordinal (CSV-style), property (JSON-style), and path (XML-style) ref-struct mappings, null/missing values, special names, zero-column shapes, materializer failures, projection-aware ordinals, rejected-row behavior, repeated/differing shape identities, runtime capability mismatch, predicates, joins, outer-null extension, grouping, windows, CTEs, and set operations. Wide shapes also verify the sealed-class carrier policy.
- Wave 6 focused hardening coverage passed 13/13. The final Release solution build passed with 0 warnings and 0 errors. The complete normal-parallel solution suite passed 17,479 tests, skipped 4 pre-existing generated-sample refresh utilities, and had 0 failures: Schema 462, Parser 1,957, Plugins 4,163, CSV 43, Git 45, Benchmarks 35, Converter 984, Evaluator 9,810. `git diff --check` passed; the initial full run completed in 117.5 seconds.
- Next step: commit Wave 6, then upgrade only the core CSV example with an opt-in query-scoped field reader/source for Wave 7.
- Wave 7 is complete: the core CSV example now has an opt-in `IQueryScopedRowSourceSchema` implementation with a concrete ref-struct field reader, generic materializer loop, raw-record pushdown before materialization, preserved diagnostics/progress/cancellation/chunking, static header/ordinal dynamic metadata discovery, and retained legacy `CsvRow`/TABLE/COUPLE behavior. New CSV coverage passed 46 tests, including typed and direct dynamic execution.
- Wave 7 Release gate passed: `git diff --check`, the normal-parallel Release build (0 warnings, 0 errors), and the complete solution suite passed 17,479 tests with 4 pre-existing generated-sample refresh skips and 0 failures. The suite completed in 110.3 seconds. Query-scoped CSV capability remains opt-in for Wave 8 qualification.
- Next step: extend the Wave 1 benchmark to compare legacy, query-scoped struct, and query-scoped class modes, then qualify and activate the CSV capability in Wave 8.
- Wave 8 is complete in the working tree: the benchmark now compares legacy rows with exact-width query-scoped readonly structs and sealed classes across 2/8/32/64 fields, selective/high-rejection/early-take scans, string and numeric data, and carrier-only materialization. The public CSV example now advertises `QueryScopedRows` by default; recorder-based tests retain an explicit legacy mode.
- Wave 8 qualification evidence: typed numeric CSV allocation was 38.5%, 47.5%, 52.7%, and 55.2% of legacy at 2/8/32/64 fields, with means of 974.61 us, 2,823.24 us, 6,970.26 us, and 5,821.30 us versus 1,569.02 us, 4,727.54 us, 13,037.36 us, and 10,326.69 us. Carrier-only struct materialization allocated 0 B; the class path allocated one carrier per row. String-only array fill did not meet a universal 2x materialization-throughput target, so the baseline records that limitation rather than overstating it; activation is justified by the end-to-end typed numeric path and allocation reduction.
- Wave 8 focused CSV coverage passed 46/46 and the benchmark project built with 0 warnings and 0 errors. The final Wave 8 Release build/full-suite gate follows before commit; BenchmarkDotNet did not produce usable per-method disassembly for the generic ref-struct fixture, while existing generated-code approval tests cover the no-boxing/no-interface-reader contract.
- Wave 9 is complete: `docs/query-scoped-dynamic-sources.md` documents the format-neutral lifecycle, CSV/JSON/XML mapping model, fallback and carrier policy, unload-safe caching, performance evidence, and explicit columnar/external-repository deferrals. `QueryScopedRowOwnershipGuardrailTests` enforces planner-owned selection, Execution IR transfer metadata, and renderer-only emission.
- Final compliance pass re-read `.github/copilot-instructions.md`. The final normal-parallel Release build passed with 0 warnings and 0 errors; the complete suite passed 17,504 tests with 4 pre-existing generated-sample refresh skips and 0 failures. `git diff --check` passed, the worktree is clean, and nothing was pushed or published.

## Current Work Unit (2026-08-18) — Query-scoped row remediation and qualification

- Remediation Waves 1-10 are committed independently as `d60b20b9b`, `d2cbfa4ce`, `7b520dd5e`, `aa3e7abe4`, `28142c336`, `877956f85`, `31e42c2c6`, `5f6a69c61`, `1abc443ca`, and `e0a4b408f`. Every wave passed focused tests, `git diff --check`, a warning-clean Release solution build, and the complete normal-parallel Release suite before commit.
- Query-row planning now uses the shared public CLR-type eligibility policy, preserves declared entities for source-injected methods, distinguishes exact empty projections from unavailable metadata, rejects ambiguous fields deterministically, and classifies scan-local versus escaping lifetimes before selecting the 64-byte struct/class policy.
- Compiled-query lifecycle coverage uses ordinal-, property-, and path-style ref-struct readers and verifies pushdown-before-materialization, accepted take limits, early disposal, cancellation, contextual failures, diagnostics, null/missing values, joins, outer joins, grouping, windows, CTEs, set operations, and chunk boundaries.
- Cache coverage proves semantic isolation across metadata shapes, capabilities, carriers, and provider instances; collectible-context and reflection ratchets prohibit static retention of generated carriers, materializers, closed generics, delegates, and row instances.
- Planner/physical/Execution IR/target/renderer invariants are covered semantically. Canonical generated samples `Q236`-`Q241` cover legacy fallback, readonly struct, sealed class, zero-field, special-name, and escaping-lifetime output; snapshot, shape, execution, profiled, interpretation, and manifest checks pass.
- The corrected benchmark contains 96 source/carrier cases and 36 compiled warm/cold cases with like-for-like correctness oracles. Three-sample medians passed all 30 activation gates: 2.4194x-15.3825x numeric struct carrier throughput, 100% struct carrier allocation reduction, 44.18%-54.39% end-to-end numeric CSV allocation reduction, bounded class allocation, all warm regression ceilings, and no boxing/interface/virtual-dispatch markers in the pinned warmed specialization.
- Wave 10 hoisted immutable `QueryRowShape` construction into the collectible generated query type, removed the remaining early-take regression, refreshed the generated corpus through repository utilities, enabled query-scoped CSV rows by default, and retained `enableQueryScopedRows: false` as the explicit legacy opt-out. Its final full suite passed 17,624 tests with 4 intentional refresh skips and 0 failures.
- Wave 11 documentation now defines exact type and projection eligibility, entity fallback, carrier lifetime, lifecycle and cache ownership, ordinal/property/path provider guidance, generated-sample maintenance, corrected benchmark evidence, an activation checklist, and the deferred external datasource/columnar/release boundaries. The CSV example README points implementers to the format-neutral guide.
- Nothing has been pushed, released, versioned, or changed in the separate `Musoq.DataSources` repository. The Wave 11 repository-wide gate and local commit follow this summary update; the final instruction audit and pinned-SDK build/test validation remain completion steps.

## Current Work Unit (2026-08-20) — Feature-coverage roadmap

- Eight commit-sized implementation waves are complete: executable feature evidence, interpretation-cache cloning isolation, primitive conversion matrices, registry-backed CTE sibling substitution, set-modifier migration diagnostics, result-level set ordering/slicing, peer-aware RANGE frames, and PERCENT_RANK/CUME_DIST.
- The executable ledger rejects specification mismatches and validates status-specific MSTest evidence, stable diagnostics, documentation, ids, and ownership. Recursive CTE, correlated set subquery, RANGE, AI, and `system.range` classifications were corrected.
- Set-result modifiers now bind and lower above complete set trees; bounded RANGE misuse reports MQ3098 during semantic analysis; distribution rankings share peer metadata and generated peer-group scans.
- Compliance re-read the repository, Parser, Evaluator, Converter, Schema, Plugins, and architecture guides. The final cleanup removed obsolete window compatibility shells and kept ranking semantics explicit across logical, physical, Execution IR, and renderer boundaries.
- Final pinned-SDK validation passed: restore, warning-clean Release build, both feature-ledger validators, and one non-overlapping full solution XPlat coverage run. The suite passed 18,315 tests, skipped exactly 4 refresh utilities, and had 0 failures in 163.5 seconds.
- Evaluator coverage improved from 85.51% line / 75.32% branch to 85.96% / 75.69%; Plugins-focused coverage improved from 72.51% / 73.20% to 80.57% / 81.97%. Nothing was pushed or published.

## Current Work Unit (2026-08-22) — Query progress and truthful phase telemetry

- Implemented all four waves of runtime query progress and phase-boundary telemetry. The public API includes `QueryProgressEventHandler`, monotonic `QueryProgressEventArgs`, configurable row/time cadence, optional `IQueryProgressSource`, typed per-run options, table progress-option overloads, contextual/profiled/async propagation, and final lifecycle flushing.
- Query progress is source-chunk based and opt-in. The no-subscriber path preserves the original `.Chunks` enumerable and reuses immutable default progress configuration without a per-run options allocation; active reporting uses atomic source counters and publication-only synchronization, with completion before `QueryPhase.End`.
- Replaced renderer phase heuristics with explicit `ExecutionPhaseBoundary` nodes. `Begin`, `From`, `Where`, `GroupBy`, `Select`, and `End` are emitted at actual clause-entry boundaries; aggregate-only queries and non-WHERE guard nodes no longer produce false phase markers. CTE and set-operation child scopes retain stable query-ID suffixes.
- Regenerated the complete tracked corpus: 250 current samples, 13 profiled samples, and the 250-entry manifest. Added shape/runtime/lifecycle coverage for empty, partial, cancelled, failed, deferred, profiled, query-scoped, CTE, set-operation, join, window, and parallel cases. `CompleteQueryProgress` is now a real reporter completion path rather than an empty generated helper.
- Added `docs/query-telemetry.md` and the streaming benchmark matrix. Corrected-build qualification on .NET 10.0.11 with 1M rows and 4096-row chunks measured 271.0 ms baseline, 274.3 ms no subscriber (1.01x), 275.3 ms active progress (1.02x), and 58.28/58.28/58.29 MB allocations respectively; this was a three-iteration ShortRun qualification, not a universal hardware guarantee.
- Final validation passed: warning-clean Release solution build; focused progress/phase/generated suites (321 tests); full Release solution suite with 10,021 Evaluator, 1,028 Converter, 4,743 Plugins, 1,962 Parser, 471 Schema, 54 Benchmark, 49 CSV, and 45 Git tests passed, with exactly 4 intentional refresh skips and 0 failures. `git diff --check` passed. Nothing was committed, pushed, or published.

## Current Work Unit — Release 17.0.8-alpha.1

- The release scope is the full five-package train: `Musoq.Parser`, `Musoq.Plugins`, `Musoq.Schema`, `Musoq.Evaluator`, and `Musoq.Converter`. The feature branch began 23 commits ahead of `origin/feature/runtime_v2_final`; the release request requires those unpublished commits and release metadata to become one commit before publication.
- Release metadata was prepared for `v17.0.8-alpha.1`, including all five central package versions, `CHANGELOG.md`, and `release-notes/v17.0.8-alpha.1.md`. The notes consolidate wildcard metadata, source parameters, set/window/apply planning, query progress, truthful phase telemetry, generated corpus, compatibility, and verification details.
- The Cloud-owned development manual audit was run in check-only mode and passed with 89 chapters and 24 checks; this core release changes no CLI or manual contract. No changes were made in `Musoq.Cloud`, `Musoq.CLI`, `Musoq.Website`, or `Musoq.DataSources`.
- Release-script tests and tag validation passed. Restore and the warning-clean Release solution build passed. The full solution gate passed 18,373 tests with 4 intentional refresh skips and 0 failures. Packing produced exactly five `.nupkg` and five `.snupkg` artifacts, including datasource ABI validation against `17.0.7-alpha.1`; clean consumer package smoke tests passed.
- The unpublished range is ready to be flattened into one release commit, then the branch and annotated `v17.0.8-alpha.1` tag will be pushed. Publication, GitHub Release creation, and exact NuGet indexing remain pending.

## Current Work Unit (2026-08-25) — Spec-negative diagnostic quality

- The canonical hand-authored spec matrix remains untouched: `TestResults/spec-query-findings.jsonl` and `.md` still contain 306 immutable, initially `unclassified` records derived only from the two named specifications; the generated-corpus report was not overwritten.
- Actionable diagnostics now cover the matrix’s previously opaque paths: MQ2037 for empty `IN`/`NOT IN`, MQ3055 for collection parameters in `VALUES`, MQ2024 for unsupported correlated value-subquery fallbacks, MQ3099–MQ3101 for window restrictions, MQ2012 for invalid/duplicate TABLE contracts, MQ3023 for undefined COUPLE tables, MQ2030 for COUPLE invocation parentheses, MQ3085 for unknown/provider source methods, and MQ3102 for TABLE/COUPLE statement order.
- Source spans are preserved through parser cloning, normalization, semantic rewrites, and source binding; statement-order validation uses the raw parser tree so diagnostics point at the offending source declaration rather than a reordered semantic stack.
- Declaration-only batches now return MQ2016 at the last declaration with explicit guidance to add a result-producing statement, replacing the prior internal stack failure.
- Focused validation passed: Parser precision/catalog 19/19, Converter diagnostic-quality/advisory 10/10, and Evaluator malformed/diagnostic/recursive/star/maintainability coverage 229/229. The Release solution build passed with 0 warnings and the full suite passed 18,519 tests (Schema 480, Parser 1,962, Plugins 4,743, Git 45, CSV 49, Benchmarks 72, Converter 1,047, Evaluator 10,121) with 3 intentional evaluator refresh skips.
- No commit, push, release, or publication was performed.

## Current Work Unit (2026-08-30) — Imported validator CORE-001 parser fixes

- Imported the seven distinct parser fixes from v6 `CORE-001` attempts 1–10: strict unsuffixed `Int32` literals, zero-digit fixed-length escape diagnostics, `ALL` keyword suggestions, invalid base-literal continuations, bracketed-identifier boundary recovery, missing-`WHEN` `CASE` diagnostics, and invalid `EXISTS` subquery classification.
- Merged overlapping attempt variants at file level and retained exact regression coverage for literal boundaries/spans, decoded and malformed escapes, valid keyword repairs, hexadecimal and binary literal continuations, reserved `CASE`, bracket recovery, and `EXISTS(1)`. Updated evaluator queries that intentionally exercise `long` arithmetic/comparisons to use the explicit `l` suffix required by the parser contract.
- Release build passed with 0 warnings and 0 errors. Parser passed 1,971/1,971; the affected evaluator slice passed 111/111; the full Evaluator suite passed 10,191/10,191 with 3 intentional refresh skips; the full solution passed 18,598 tests with 3 intentional skips and 0 failures.
- Validator run metadata, clean attempts, unrelated worktrees, and other repositories were not imported. No commit, push, release, or publication was performed.
# Current Work Unit (2026-09-12) — Structured input remediation Wave 10

- Wave 10 removes the executable fallback that silently routed structural parameters through the boxed normalizer when a target lacked generated capture. `CaptureForExecution` now requires `IStructuralParameterSnapshotProvider` for structural definitions while retaining scalar compatibility. Host record capture no longer reflects over dictionary `Key`/`Value` properties; supported indexed/dictionary representations are handled directly and unsupported shapes fail clearly.
- Added capability and source-scan regressions, while retaining atomic snapshot, cancellation, lifecycle, mutable-input, and cross-run ownership coverage already present in the maintained example tests. Focused evaluator and structured ownership tests pass; the final unchanged-tree Release gate, runner, summary refresh, and one Wave 10 commit remain required.
# Current Work Unit (2026-09-12) — Structured input remediation Wave 11

- Wave 11 removed the manual node-limit rejection from `StrictLimitSource`, added a decimal typed-source overload and an end-to-end widening probe, and added an invalid nullable-element resource that preserves the existing `MQ3088` binding contract. The generated numeric overload sample now compiles a decimal literal and proves the decimal constructor is selected; snapshots and the manifest were refreshed with the repository utility.
- Focused maintained-example tests pass 28/28, including source-owned limit removal and nullable rejection. The final unchanged-tree Release gate, runner, summary refresh, and one Wave 11 commit remain required.

## Current Work Unit (2026-09-12) — Query rewrite optimization waves W00-W03

- W00-W02 established the measured LIKE path: a shared conservative classifier, direct `StringComparison.OrdinalIgnoreCase` execution helpers, Execution IR specialization, faithful C# rendering, generated-sample coverage, and the compiled-query BenchmarkDotNet baseline.
- W03 adds an opt-in versioned `Musoq.Schema.Optimization` contract for typed exact/prefix/suffix/contains predicates, explicit row-filtering or candidate-metadata application phases, immutable execution-plan metadata, artifact snapshots, and provider capability negotiation. Unsupported or unknown capabilities stay residual; only safe top-level `AND` conjuncts are split.
- Core source planning now preserves simple constant LIKE nodes for conversion, validates accepted typed applications against advertised capabilities, restores deferred predicates, emits an unknown-version diagnostic, and keeps the existing generic matcher for interior `%`, `_`, non-ASCII, and cross-source shapes. DataSources/Search provider-specific advertising remains a separate owner boundary.
- W03 focused capability, planner, visitor, physical-plan, artifact, generated-sample, and maintainability coverage passed. The final warning-clean Release build passed, and the complete solution suite passed 21,319 tests with 3 intentional skips and 0 failures. Three final benchmark cohorts qualified the specialized compiled-query path on the same machine; raw BenchmarkDotNet output remains ignored.
- Nothing was pushed, published, tagged, or changed in the separate `Musoq.DataSources` repository.

## Current Work Unit (2026-09-12) — Constant LIKE corrective waves R00-R01

- R00 retains the benchmark-only historical baseline on local branch `qms-like-baseline-completion` at `cb4d781c3` and carries the identical expanded benchmark and gate surface forward in Core commit `8d078bfa4`. The query matrix covers exact, prefix, suffix, contains, multiple matches, `_`, interior `%`, non-ASCII patterns, dynamic patterns, Unicode inputs, and candidate payload work at 10k and 100k rows.
- R01 replaces nullable classification with stable rejection reasons and restores the W00 regex result contract for non-ASCII input. Classified ASCII patterns use ordinal matching only when the relevant comparison span is ASCII; otherwise execution falls back to the legacy cached regex matcher after trivial false and empty-contains checks.
- Focused classifier, differential Unicode, null, culture, regex-metacharacter, repeated-wildcard, and compiled-query tests pass 48/48. No DataSources repository changes, remote operations, publication, tags, or releases are part of this campaign.
- R02 introduces the standalone `ExecutionStringMatch` operation with original pattern, classified needle, kind, `LikeIgnoreCase` semantics, traversal/rewriting/analysis, plan text, stability, CSE, aggregate fingerprints, substitutions, parallel eligibility, and explicit target feature reporting. Execution IR is now v6; Host ABI remains v6 and package format remains v2.
- The C# target renders each input once through a collision-safe pattern variable, performs direct ordinal BCL matching on safe ASCII spans, and calls the static legacy-regex fallback for non-ASCII spans. The portable subset rejects `expr.string-match` and its two feature kinds explicitly. Focused renderer/IR tests pass 146/146 and target/portable tests pass 59/59; normal SQL lowering does not produce the new node until R03.
- R03 replaces the temporary `ExecutionPatternMatch.Specialization` metadata with actual `ExecutionStringMatch` nodes after CTE sidecar lowering and before reuse, LICM, and CSE. Generic `LIKE` remains for dynamic patterns, `_`, interior `%`, and non-ASCII patterns; deterministic optimizer trace text reports each fallback reason and Execution Plan text records pattern, needle, kind, and comparison.
- The old specialization types and renderer branch are removed. Compiled constant-ASCII queries now emit direct BCL matching with static Unicode fallback and no generated `.Like(...)` or `new Operators()` call; negation, quantifier expansion, rewrites, fingerprints, parallel eligibility, and single-evaluation rendering are covered. Q269 and its manifest entry were refreshed through the repository utilities. Focused evaluator tests pass 110/110, converter tests pass 3/3, and generated-sample/manifest tests pass 372 with the two intentional refresh skips.
- R03 is committed as `ac8673955`; its warning-clean Release build and complete nine-project suite passed with 11,919 Evaluator tests, three intentional refresh skips, and no failures.
- R04 corrects the unreleased Schema v1 contract: `LikeIgnoreCase` denotes legacy-result equivalence, typed matches preserve original pattern plus classified needle, applications are strongly typed, collection inputs are frozen, direct top-level `AND` multiplicity is preserved without traversing `OR`, and capability validation rejects non-positive versions, null entries, invalid flags/phases, and case-insensitive duplicate columns while allowing unusable positive future versions. Public XML comments, API-shape, immutability, version, duplicate, and phase-boundary documentation coverage are included; focused Schema/Evaluator/converter tests pass.
- R04 is committed as `c44c01fe7`; its warning-clean Release build and complete suite passed with Schema 567 and Evaluator 11,920, three intentional refresh skips, and no failures.
- R05 negotiates each flattened top-level `AND` conjunct independently. Direct typed matches cross the provider boundary only when every capability dimension is advertised; matches nested in `OR` or any other expression are deferred as a whole. Applications are validated one-for-one against the accepted direct-match multiset, then deferred and provider residuals are merged and the original multiset is revalidated.
- Planning Text now emits pattern, kind, needle, comparison, negation, accepted phase, and an explicit stable fallback reason. Positive nested and duplicate-multiplicity coverage passes, and malicious missing, duplicate, altered, nested, unknown-version, and unadvertised-phase applications all fail during compilation before source execution. Focused R05 coverage passes 91/91.
- R06 gives `CandidateMetadata` its executable phase boundary: typed applications prune cheap candidate values before projection/payload work, while `RowFiltering` applications run only after materialization and the complete accepted predicate remains the post-materialization safety check in the shared source executor.
- A dedicated lazy Core source independently counts payload opens, decodes, row materializations, and enumerator disposal. Nineteen focused scenarios prove zero-match/partial-match exact counts, rejected-versus-accepted row order and hash parity, null and Unicode paths, positive/negated matches, unsupported columns/shapes/versions, partial `AND`, supported-looking `OR`, cancellation, early disposal, contextual open/decode failures, and invalid applications failing with every payload counter at zero.
- The candidate BenchmarkDotNet on arm now advertises the `Name` metadata capability, accepts the typed match at `CandidateMetadata`, and evaluates it before simulated payload access. A benchmark contract test proves 10,000 baseline payload opens versus exactly 1,000 optimized opens with identical ordered IDs and payloads. The retained local baseline ref advanced without rewriting to benchmark-only commit `297e34089` so its historical off/on arms use the same non-empty `entity-000%` workload.
- R06's warning-clean Release gate passed 21,372 tests with three intentional generated-sample refresh skips and no failures: Schema 567, Parser 2,694, Plugins 4,743, Git 45, CSV 49, structured inputs 51, Converter 1,165, Benchmarks 110, and Evaluator 11,948. `git diff --check` passed; no external repository or remote was changed.
- R07 adds the normative Core source string-match v1 provider guide covering legacy-result matching semantics, direct-conjunct negotiation, exact residual ownership, candidate-versus-row phase obligations, Unicode fallback, failures/cancellation/disposal, counters, a minimal provider outline, and a qualification checklist. The evaluator performance chapter now labels the original four-shape table as incomplete historical characterization and points to the final corrective receipt.
- The standalone Search handoff is `docs/handoffs/musoq-datasources-search-string-match-v1.md`, titled `feat(search): consume Core string-match v1 for pre-open path pruning`. It requires metadata-backed columns, Core-equivalent matching, filtering before content open/decompression, retained I/O counters, and explicitly makes no traversal-pruning claim. No Musoq.DataSources files or W21 campaign state were changed. Focused documentation/capability/pruning coverage passes 33/33.
- R07's warning-clean Release gate passed the unchanged 21,372-test matrix with three intentional generated-sample refresh skips and no failures. `git diff --check` passed; the documentation wave is ready for its standalone commit.
- The first R08 local comparison rejected completion: enabled ASCII shapes passed, including 100k contains at 0.7068x baseline time, but dynamic/generic noise and the Unicode workload exceeded the 1.03x fallback ceiling. The Unicode benchmark had also used Kelvin sign `K`, for which the historical tree's pre-campaign ordinal shortcut and the required legacy-regex semantics return different row sets, invalidating parity and allocation comparison.
- A forward corrective change now evaluates the direct ordinal operation before the ASCII guard and invokes the legacy matcher only after a direct miss with non-ASCII comparison input; the guard uses the BCL span range intrinsic rather than a scalar custom loop. Fixed differential tests still cover the Kelvin/Turkish-I semantic cases. The benchmark Unicode-input workload now uses non-ASCII `Ł` plus ASCII `K`, exercising fallback while preserving identical historical/current results; the retained baseline ref advanced without rewrite to benchmark-only commit `e980b09c9`. Focused semantic/renderer coverage passes 170/170.
- The first corrective full gate had no behavioral failures but correctly rejected stale generated artifacts after the renderer expression changed. Q269 and the manifest were refreshed through the intentional repository utilities; the complete 378-test generated snapshot/manifest/duration slice now passes. A full corrective gate rerun follows before commit.
- The corrective Release build passed with zero warnings. The first full rerun suffered an unrelated CLR test-host crash inside `GC.Collect()` in `CollectibleAssembly_ShouldNotBeRetainedByReferenceDiscoveryState` after 10,046 evaluator passes and no assertion failures; all other projects passed. The complete evaluator project was rerun unchanged and passed 11,948 tests with three intentional skips, restoring the full 21,372-test qualification matrix without weakening or filtering the gate.
- Three corrected local cohorts on baseline `e980b09c9` and current `0d144b24e` passed every enabled shape, generic wildcard fallback, and Unicode fallback, but still measured the dynamic-pattern runtime helper at 1.0386x-1.0459x, above the fixed 1.03 ceiling. The remaining cost was the short ASCII-span guard after direct misses. A second forward correction adds an aggressively inlined scalar short-span path for up to eight UTF-16 code units while retaining the BCL range intrinsic for longer spans; it is not custom SIMD and does not alter fallback semantics. A diagnostic two-case ShortRun measured 1.0081x at 10k and 1.0003x at 100k against the three baseline medians; formal cohorts will be rerun only after the corrective full gate and commit.
- The second correction's warning-clean Release gate passed all 21,372 tests with three intentional generated-sample refresh skips and no failures. `git diff --check` passed; formal measurements must use the resulting committed SHA and fresh current reports.
- Formal local qualification on current `473ca0bf1` passed all 20 local/fallback cases against baseline `e980b09c9`: dynamic-pattern time was 1.0055x/1.0244x, Unicode-input time 0.9931x/0.9049x, and 100k contains time 0.4860x; all allocations passed and no specialization kind required removal.
- The first candidate-payload launch exposed a benchmark-construction defect before measurement: BenchmarkDotNet rejects benchmark methods declared by a sealed class. Both forward-only refs now make only that fixture class unsealed; focused benchmark coverage passes and the retained baseline ref advanced to benchmark-only commit `8d5bdd043`. No failed-launch artifact will enter qualification.
- The benchmark-runnability correction's warning-clean Release gate passed all 21,372 tests with three intentional skips and no failures. Candidate qualification must use the resulting committed Core SHA and baseline `8d5bdd043`.
- The first candidate qualification reports disproved the harness assumption: current-side `[IterationSetup]` forced one invocation while the retained baseline used adaptive invocation counts, and the discarded simulated payload hash allowed the JIT to erase the work being used as the pre-open cost. The resulting reports are excluded from qualification.
- The corrected harness makes the payload hash observable on both trees, keeps the baseline's nominal off/on arms at `RejectProjection`, and removes current-only BenchmarkDotNet iteration setup. Candidate-phase evaluation now prepares column selectors once, avoids a per-row LINQ closure, uses classified kind/needle ordinal operations for ASCII spans, and falls back to legacy regex semantics only for relevant non-ASCII input. The retained baseline advanced forward to benchmark-only SHA `ff94e49d6`.
- Focused candidate tests pass 26/26. They cover all four specialized kinds against Kelvin/Turkish-I legacy Unicode behavior and verify both 10k and 100k benchmark workloads: baseline payload opens equal candidate count, optimized payload opens are exactly 1,000, row order/payloads match, and both arms produce SHA-256 `E01E398494E8A0FB7094309FBBF66574308CF0AC034748B46B4091C857440178`.
- The candidate-execution corrective gate passed a warning-clean Release build and all 21,378 solution tests with the three intentional generated-sample skips and no failures. The diagnostic cohort measured the corrected 100k historical on arm at 12.807 ms and candidate pruning at 2.792 ms; these diagnostic reports are not part of the final three-cohort receipt.
- Final three-cohort qualification passed both repository gates without weakened thresholds. Local reports compare `e980b09c9` with `473ca0bf1`; candidate reports compare `ff94e49d6` with `84ff8977b`. The 100k contains ratio is 0.4860x, the worst fallback ratio is 1.0244x, and candidate time is 0.3160x/0.1938x with exact 10,000-to-1,000 and 100,000-to-1,000 payload-open reductions. All result counts and SHA-256 hashes match. The final receipt is `docs/campaigns/qms-like-specialization-qualification.md`.
- The mandated penultimate compliance audit re-read the repository, Evaluator, Schema, Benchmark, and architecture guides in full. Ownership, explicit IR, renderer fidelity, public-contract documentation, benchmark equivalence, generated evidence, and maintainability rules are satisfied. R08 focused Schema, Benchmark, and Evaluator suites passed 63/63; the standalone receipt commit and exact committed-tree final Release gate remain.

## Current Work Unit (2026-09-13) — Dynamic LIKE campaign QMS-R09

- QMS-R09 adds an executable SQL `LIKE` conformance catalog with stable IDs, hard-coded schema and ordered-row snapshots, pattern/source/domain/polarity/null/context metadata, and expected direct/prepared/dynamic strategy metadata. Its 133 focused checks cover every planned pattern shape, every non-APPLY pattern source, ASCII and Unicode edge families, both polarities, the full null matrix, and invariant/en-US/pl-PL/tr-TR execution. Cache-sensitive cases execute twice.
- The catalog exposed the known culture-sensitive matcher-cache defect. In accordance with the campaign rule to fix discovered bugs in the same wave, both generic and legacy LIKE caches are now keyed by original pattern plus preparation culture. A dedicated cache regression proves invariant and en-US matchers for dotted I remain separate.
- Added `docs/campaigns/qms-like-query-conformance-crosswalk.md`; `RLIKE` and `SELECT * LIKE` remain outside the operator catalog. JOIN/APPLY/aggregate/window/CTE/set/parallel context coverage remains QMS-R10 work.
- Focused conformance/cache tests pass 136/136. `git diff --check` and the warning-clean Release build pass. The complete Release solution gate passes: Schema 567, Parser 2,694, Plugins 4,743, Git 45, CSV 49, structured inputs 51, Converter 1,165, Benchmarks 111, and Evaluator 12,087 passed with three intentional generated-sample refresh skips.
- No push, tag, release, history rewrite, or change outside `D:\repos\Musoq` was performed.

## Current Work Unit (2026-09-13) — Dynamic LIKE campaign QMS-R10

- QMS-R10 closes all 18 query-context dimensions with exact schema/ordered-row assertions: projection, WHERE/boolean composition, CASE, inner/left JOIN ON, correlated CROSS/OUTER APPLY, aggregate FILTER, HAVING, direct QUALIFY composition, filtered windows, ordinary/recursive CTEs, UNION ALL, distinct/order/page, quantifiers, and parallel CTE branches.
- The required `m1.Column like m2.Column` correlated CROSS APPLY query is executable and covered together with reverse orientation, chained APPLY, empty children, OUTER APPLY null extension, negation, Unicode, and exact correlated source invocation order/counts.
- The context suite exposed and fixed two production defects in the same wave. The parser now accepts qualified expressions on the right of LIKE/NOT LIKE/RLIKE/NOT RLIKE, with four parser regressions. Aggregate finalization now recognizes and lowers pattern matches in HAVING; the conversion is split into `AggregateFinalPatternMatchConversion` so the tracked physical-lowering family remains at its established size ceiling.
- Focused parser/evaluator coverage passes 299/299. `git diff --check`, the warning-clean Release build, and the complete Release suite pass: Schema 567, Parser 2,698, Plugins 4,743, Git 45, CSV 49, structured inputs 51, Converter 1,165, Benchmarks 111, and Evaluator 12,108 passed with three intentional generated-sample refresh skips.
- One full-suite attempt observed a non-reproducible RLIKE timeout in an existing raw-path test under concurrent project load. The exact test passed immediately in isolation, and the complete unchanged-tree rerun passed; no timeout contract was changed.

## Current Work Unit (2026-09-13) — Dynamic LIKE campaign QMS-R11

- QMS-R11 adds compiled-query benchmark matrices for dynamic pattern cardinalities 1/2/64/512/4096 across ASCII, Unicode, and wildcard domains; input lengths 4/64/4096; correlated APPLY fan-outs 1/8/64 in both pattern orientations; and dynamic/APPLY compilation. Deterministic result hashes, source invocations, child-collection reads, matcher-construction behavior, generated C#, and Execution IR shape are guarded by tests.
- `gate-dynamic-like` enforces the campaign's low/medium/high cardinality, input-length, inner/outer APPLY, Unicode/wildcard, constant-ASCII, allocation, and compilation thresholds from three baseline/current reports. Gate boundary and failure tests pass.
- The first benchmark launch exposed sealed benchmark fixtures; all four fixtures are now unsealed and a dedicated BenchmarkDotNet-runnability regression protects them. A later 4,096-code-unit Unicode case exposed .NET's non-backtracking automaton node limit. Dedicated direct exact, wildcard, and compiled-query regressions now pass; rejected non-backtracking construction alone falls back to the existing timeout-bounded compiled regex semantics.
- Three corrected isolated 52-case ShortRun cohorts completed without errors. Full median means, allocations, environment, commands, artifact paths, result hashes, work counters, and generated-shape evidence are committed in `docs/campaigns/qms-dynamic-like-baseline.md`; all five three-report cohorts parse and self-compare successfully.
- Focused LIKE coverage passes 323/323 and dynamic benchmark/gate coverage passes 18/18. The retained local `qms-dynamic-like-baseline` ref still points at the R10 parent until the standalone R11 commit is created; it will then advance to that corrected baseline without push or history rewrite.
- `git diff --check`, Release restore, and the warning-clean Release solution build pass. The complete Release suite passes with no failures: Schema 567, Parser 2,698, Plugins 4,743, Git 45, CSV 49, structured inputs 51, Converter 1,165, Benchmarks 129, and Evaluator 12,111 passed with three intentional generated-sample refresh skips.

## Current Work Unit (2026-09-13) — Dynamic LIKE campaign QMS-R12

- QMS-R12 separates simple LIKE shape from character domain. Classifications now retain exact/prefix/suffix/contains independently from ASCII/Unicode, including the domain on unsupported `_` or interior-`%` shapes. Constant `ExecutionStringMatch` lowering and source-predicate transfer remain conservatively ASCII-only; the existing `non-ascii-pattern` optimizer fallback trace remains stable.
- Cached Unicode simple-shape matchers are prepared under the culture included in the cache key. They may return directly only for a case-sensitive ordinal exact/prefix/suffix/contains hit; every miss uses the captured historical IgnoreCase regex matcher. ASCII behavior and the legacy specialized-input fallback remain unchanged.
- A deterministic generated Unicode-pattern corpus and fixed Kelvin, Turkish-I, sigma, composed/decomposed, and surrogate cases compare generic LIKE against a freshly constructed historical regex under invariant, en-US, pl-PL, and tr-TR cultures. A dedicated planner test proves Unicode patterns remain evaluator residuals.
- Focused classifier/planner/optimizer/catalog/differential coverage passes 171/171; all LIKE-named evaluator tests pass 334/334, and the dynamic benchmark result-hash suite passes 15/15. Full Release validation and the standalone R12 commit remain.
- `git diff --check`, Release restore, and the warning-clean Release solution build pass. The complete Release suite passes with no failures: Schema 567, Parser 2,698, Plugins 4,743, Git 45, CSV 49, structured inputs 51, Converter 1,165, Benchmarks 129, and Evaluator 12,122 passed with three intentional generated-sample refresh skips.

## Current Work Unit (2026-09-13) — Dynamic LIKE campaign QMS-R13

- QMS-R13 replaces per-write dictionary snapshot publication in `BoundedRuntimeCache` with lock-free `ConcurrentDictionary` hit reads and a serialized miss/replacement/FIFO-eviction path. Existing bounded capacity, exactly-once factory execution per key, insertion-order eviction, freshness replacement, and clear behavior remain covered; a blocked miss now has a deterministic regression proving it cannot stall an unrelated cached hit.
- Dynamic LIKE now enters through static `Operators.LikeDynamic`; the instance `Like` method remains a compatibility wrapper. Cached values are immutable `PreparedLikeMatcher` instances retaining the original SQL pattern and literal offsets. ASCII direct matching uses spans over the original pattern, and Unicode or unsupported shapes retain their prepared historical matcher, so ASCII Unicode-input fallback no longer allocates reconstructed percent-wrapped patterns.
- Added focused coverage for repeated-boundary offsets, unsupported wildcard fallback, Kelvin-sign legacy equivalence, null operands, alternating ASCII/Unicode/wildcard patterns, concurrent cache access, stale replacement, FIFO ordering, bounded high-cardinality churn, and culture-separated matcher entries. Focused evaluator LIKE/cache coverage passes 347/347 and benchmark LIKE contracts pass 22/22.
- `git diff --check`, Release restore, and the warning-clean Release build pass. The complete Release solution gate passes 21,577 tests with no failures: Schema 567, Parser 2,698, Plugins 4,743, Git 45, CSV 49, structured inputs 51, Converter 1,165, Benchmarks 129, and Evaluator 12,130 passed with three intentional generated-sample refresh skips.

## Current Work Unit (2026-09-13) — Dynamic LIKE campaign QMS-R14

- QMS-R14 models matcher preparation, prepared matching, dynamic matching, and execution-local two-entry cache slots as explicit Execution IR operations. The four operations participate in traversal, rewriting, alias handling, outer-apply null substitution, stability, aggregate and hoist fingerprints, CSE, window row-scope analysis, parallel eligibility, operation/plan printing, and target feature analysis. Normal query lowering remains unchanged for this wave.
- CSharpClr validates and renders the new operations through static `Operators.PrepareLike`, `Operators.LikePrepared`, and `Operators.LikeDynamic` calls plus a per-region `LikeMatcherCacheSlot`. The portable test target rejects every new operation and the `LikeMatcherStrategy` feature before rendering. Execution IR and Host ABI contracts are version 7, package format remains 2, compiled artifact format remains 3, and the derived custom ABI import is `prepared-and-dynamic-like` / `like-matcher-v1` with capacity, comparison, and culture metadata.
- The wave's semantic audit found three matching defects and each has dedicated regression coverage in this wave. Provider documentation incorrectly described LIKE as culture-independent; it now specifies preparation-culture semantics. A first prepared fallback failed to reproduce historical regex handling of dotted I under en-US; prepared ASCII fallback now lazily constructs the historical regex under the captured culture. Existing direct ordinal ASCII positives were incorrect under tr-TR (`i LIKE 'I'`); direct helpers, generated string-match code, prepared matchers, and the candidate-metadata test source now guard only I-sensitive literals and otherwise retain the fast path.
- Generated Q269 was refreshed twice through the intentional repository utilities while diagnosing the culture guard. The final literal-sensitive renderer leaves Q269 and its manifest unchanged because its needles contain no I/i, and emits exactly one runtime culture check for an I-sensitive literal. Dedicated renderer tests enforce both outcomes.
- The first full gate exposed repository architecture-contract failures rather than query-result failures: registrations had grown capped partial families, the four new public expressions were absent from the intentional portable-IR inventory, and the new host import used custom/raw details. Pattern-expression traversal is now centralized in `PatternExpressionFacts`, matcher rendering/validation in `ExecutionLikeMatcherSyntaxFactory`, capped family totals pass unchanged, the public inventory records 47 expressions and its new hash, and `TargetLikeMatcherAbiDetails` provides typed validated ABI metadata with a non-positive-capacity regression.
- Focused R14 validation passes: 711 evaluator renderer/IR/runtime/culture/candidate/snapshot/architecture checks with two intentional refresh skips, 12 converter target/package checks, and 15 dynamic benchmark contract checks. `git diff --check`, Release restore, and the warning-clean Release solution build pass. The complete corrected solution gate passes 21,597 tests with no failures: Schema 567, Parser 2,698, Plugins 4,743, Git 45, CSV 49, structured inputs 51, Converter 1,166, Benchmarks 129, and Evaluator 12,149 passed with three intentional generated-sample refresh skips.

## Current Work Unit (2026-09-13) — Dynamic LIKE campaign QMS-R15

- QMS-R15 replaces constant-only specialization with `LikeStrategyLowering`: simple ASCII constants become direct `ExecutionStringMatch`, all other constants become deduplicated execution-root `ExecutionPrepareLikeMatcher` plus `ExecutionPreparedLikeMatch`, and every dynamic pattern becomes `ExecutionDynamicLikeMatch`. No lowered SQL LIKE path emits `new Operators().Like`; instance `Like` remains only as a compatibility API and RLIKE remains separate.
- Dynamic expressions share one two-entry cache per serial region. Parallel task bodies own independent serial slots, while filter/project and aggregate workers use a worker-local slot whose query-local shell partitions state by managed worker thread. Prepared matchers remain immutable and are prepared once per distinct constant for the execution, including constants repeated across parallel tasks.
- Three scope bugs found during the wave have dedicated regressions and fixes. Dynamic APPLY projection initially referenced cache state moved into a stored CTE helper; matcher state is now emitted with execution-state declarations. Filtering those declarations then made stored-helper member and call-site capture discovery disagree; both discovery paths now use the same filtered block. Prepared constants were initially duplicated per parallel task; preparation is now deduplicated at the execution root.
- The executable conformance slice and dynamic benchmark contracts pass after lowering. New generated samples Q369-Q371 cover dynamic columns, correlated APPLY, and Unicode WHERE/projection/aggregate FILTER/HAVING contexts; Q269 remains covered. The tracked corpus and 369-entry manifest were refreshed through the repository utilities, and shape tests reject any generated instance LIKE call.
- One concurrent full-solution attempt reproduced the repository's prior internal CLR test-host abort after 12,139 evaluator passes and no assertion failures. The unchanged complete Evaluator project then passed 12,160 tests with three intentional skips, and the exact full-solution rerun passed 21,608 tests with no failures: Schema 567, Parser 2,698, Plugins 4,743, Git 45, CSV 49, structured inputs 51, Converter 1,166, Benchmarks 129, and Evaluator 12,160. `git diff --check`, restore, and the warning-clean Release build pass.

## Current Work Unit (2026-09-13) â€” Dynamic LIKE campaign QMS-R16

- QMS-R16 admits LIKE-bearing property/APPLY pipelines to the existing single-use projection fusion without broadening unrelated APPLY queries. Once fused, stable outer-row fields, stable member reads, outer locals, script parameters, and script variables can own a deduplicated `ExecutionPrepareLikeMatcher` before dependent inner setup; inner-row patterns, volatile fields, conditional bodies, scoped/materialization/recursive/window boundaries, and parallel regions remain dynamic.
- The required orientations are explicit in generated artifacts: `m1.Input like m2.Pattern` retains `ExecutionDynamicLikeMatch`, while `m2.Value like m1.Pattern` binds `m1`, prepares once, opens `m1.Children`, binds `m2`, and calls `Operators.LikePrepared`. Runtime benchmark guards prove one child-source read per outer row, zero outer-pattern reads for the inner-pattern query, and exactly one outer-pattern read per outer row for the prepared query.
- Two defects found during the wave have dedicated regressions and fixes. Root matcher declarations depending on script parameters were emitted before parameter locals; LIKE state now renders after parameter/variable bindings in normal, final-shape, and typed-sink entry paths. The renderer split initially exceeded both architecture-family ceilings by eight lines; the implementation was compacted and split without raising either budget.
- APPLY result tests cover CROSS/OUTER APPLY, null extension, source counts, cancellation before pattern evaluation, and contextual execution exceptions with the original cause. Optimizer tests cover deduplication, dependency-ready placement, reverse orientation, parameter ownership, conditional/scoped boundaries, volatile fields, and stable member reads. The three LIKE generated samples and their manifest were refreshed through the intentional repository utilities.
- Focused qualification passes 374 evaluator LIKE tests, 23 benchmark LIKE contracts, and 675 generated-sample checks with two intentional refresh skips. The first full gate both identified the repaired renderer-family budget and reproduced the repository's prior internal CLR test-host abort after 12,151 evaluator passes; after the budget fix, the exact full Release rerun passes 21,621 tests with no failures: Schema 567, Parser 2,698, Plugins 4,743, Git 45, CSV 49, structured inputs 51, Converter 1,166, Benchmarks 130, and Evaluator 12,172 passed with three intentional refresh skips. `git diff --check`, restore, and the warning-clean Release build pass.

## Current Work Unit (2026-09-13) — Dynamic LIKE campaign QMS-R17

- QMS-R17 classifies runtime LIKE patterns in one allocation-free indexed pass. Accepted classifications retain offsets into the original pattern rather than allocating a literal substring; compile-time direct IR and source-predicate lowering materialize a needle only at their existing ownership boundaries.
- Historical regex source construction is now one checked pass with canonical escaping and wildcard expansion, replacing the generated-regex escape plus two replacement strings. Dedicated regressions cover every escaped metacharacter and complete 4,096-character wildcard expansion.
- The execution-local two-entry cache has a test-only matcher factory seam. High-cardinality coverage proves 4,096 distinct patterns remain bounded at two entries with exactly one factory call and matcher construction per miss; separate mixed ASCII, Unicode, interior-wildcard, underscore, and raw-path churn compares every result with a freshly constructed historical regex. Four concurrent worker states each churn 512 unique patterns and remain bounded at two entries per worker with exact construction counts.
- Focused R17 validation passes 45 classifier/prepared/cache/semantic tests, all 380 LIKE-named evaluator tests, all 23 LIKE benchmark contracts, and 276 maintainability/architecture tests. `git diff --check`, Release restore, and the warning-clean build pass. The first complete-suite attempt reproduced the repository's prior internal CLR test-host abort after 12,156 Evaluator passes and zero assertion failures; the unchanged exact rerun passes 21,627 tests with no failures: Schema 567, Parser 2,698, Plugins 4,743, Git 45, CSV 49, structured inputs 51, Converter 1,166, Benchmarks 130, and Evaluator 12,178 passed with three intentional generated-sample refresh skips.

## Current Work Unit (2026-09-13) — Dynamic LIKE campaign QMS-R18

- QMS-R18 adds a 64-case BCL ASCII-kernel matrix covering lengths 1/4/8/9/16/64/256/4,096 and all-ASCII or first/middle/last non-ASCII input. Three isolated complete cohorts compare `Ascii.IsValid` with `ContainsAnyExceptInRange`; both allocate zero bytes in every case.
- Median-of-three qualification selects `Ascii.IsValid`: its 12-case long-input geometric mean is 0.3532x the current primitive and its worst result across all 32 parameter pairs is 0.5053x, passing the required 0.95 improvement and 1.03 no-regression gates. Production retains the scalar path through the eight code units, now explicitly named `IsAsciiUpToEightCodeUnits` with `ScalarAsciiProbeLimit = 8`, and delegates longer spans to `Ascii.IsValid`.
- Enabled AVX2 disassembly shows BCL YMM operations for both candidates and measures 28.88 ns versus 102.79 ns at 4,096 ASCII code units. With `DOTNET_EnableHWIntrinsic=0`, BenchmarkDotNet reports no hardware intrinsics or vector registers and measures 99.12 ns versus 1,737.17 ns, proving a viable scalar fallback. No handwritten `Vector128`, `Vector256`, or `Vector512` code was added. The reproducible receipt is `docs/campaigns/qms-bcl-simd-like-kernels.md`; raw artifacts remain ignored.
- Dedicated production probe regressions cover the 8/9 boundary, long inputs, non-ASCII positions, and selected-subspan isolation. Focused R18 validation passes all 394 LIKE-named evaluator tests, 26 LIKE benchmark tests, and 276 maintainability/architecture tests. `git diff --check`, restore, and the warning-clean Release build pass. The first complete-suite attempt reproduced the prior internal CLR test-host abort after 12,169 Evaluator passes and zero assertion failures; the unchanged exact rerun passes 21,644 tests with no failures: Schema 567, Parser 2,698, Plugins 4,743, Git 45, CSV 49, structured inputs 51, Converter 1,166, Benchmarks 133, and Evaluator 12,192 passed with three intentional generated-sample refresh skips.

## Current Work Unit (2026-09-13) — Dynamic LIKE campaign QMS-R19 corrective runtime qualification

- The first three current cohorts correctly blocked qualification. They exposed process-cache bypass during matcher preparation, very large Unicode cardinality regressions, constant Unicode/wildcard setup cost, a stale benchmark model of the 512-entry historical cache, and low-cardinality dynamic wildcard overhead. Each production or evidence defect has a dedicated regression in this wave.
- Expensive Unicode and wildcard preparations now reuse the bounded culture-keyed process cache, while simple ASCII preparations stay execution-local so 4,096-cardinality churn does not reintroduce lock/eviction cost. The legacy regex path has a single immutable front entry, retains bounded backing storage, chooses a timeout-bounded optimized regex for safe simple shapes, and retains the non-backtracking historical form for repeated/interior/empty wildcard cases.
- ASCII-specialized misses use a conservative culture-aware Unicode prefilter before invoking the historical regex. An exhaustive regression checks every UTF-16 code unit against A-Z under invariant, en-US, pl-PL, and tr-TR and proves the prefilter never rejects a historical-regex positive. Kelvin sign, dotted/dotless I, culture separation, matcher reuse, regex-source, and engine-selection regressions pass.
- Generated constant one-code-unit matches use a scalar ASCII kernel and the conservative fallback. Generated dynamic matches capture both operands exactly once in a left-to-right tuple pattern and inline the common one-code-unit prefix positive path; every ambiguous or unsupported shape calls `Operators.LikeDynamic` and its two-entry execution-local cache. The broader LIKE suite found and fixed an initial runtime shortcut that bypassed cache construction/FIFO/worker-local contracts; dedicated cache tests now pass.
- Q269, Q369, and Q370 plus the tracked manifest were refreshed through the intentional repository utilities. Focused validation passes 417 LIKE-named evaluator tests, 135 benchmark tests, 379 generated snapshot/manifest/duration checks with two intentional refresh skips, and 276 architecture/maintainability checks. `git diff --check`, restore, and the warning-clean Release build pass.
- The first complete gate reported only the expected stale generated evidence and then reproduced the known CLR test-host abort after 12,167 evaluator passes. After the intentional refresh, the unchanged exact full rerun passes 21,670 tests with no failures: Schema 567, Parser 2,698, Plugins 4,743, Git 45, CSV 49, structured inputs 51, Converter 1,166, Benchmarks 135, and Evaluator 12,216 passed with three intentional refresh skips.
- Diagnostic query benchmarks are not qualification evidence. Unicode input at 100k improved from the historical 6.755 ms median to 5.638 ms in the latest focused diagnostic (0.835x, inside the 0.85 gate). Low-cardinality wildcard diagnostics measure around 0.90x but remain close enough to the fixed threshold that only three fresh cohorts on the committed corrective SHA may qualify the wave.
- The first committed corrective qualification cohort on `c93142233` was rejected rather than averaged with further runs: wildcard cardinalities 1/2 measured 0.9115x/0.9234x, Unicode input at 100k measured 0.8631x, and plain/APPLY compilation measured 1.1707x/1.3467x, outside the unchanged gates. Cohorts two and three were not run for that superseded implementation.
- The follow-up correction removes the large inline dynamic expression from generated C# and keeps one compact left-to-right `Operators.LikeDynamic(input, pattern, slot)` call. Its two-entry cache now owns the safe one-ASCII-prefix hit path after validating pattern and culture, including equal non-reference patterns; a dedicated regression proves equivalent pattern instances reuse one matcher instead of bypassing cache accounting.
- The Unicode prefilter now handles the only invariant non-ASCII positives for ASCII K/S explicitly (Kelvin sign and long s) and reserves culture-sensitive casing for I/i. The exhaustive culture differential remains green, and a focused 100k diagnostic measured 5.574 ms, below the unchanged 5.742 ms Unicode threshold.
- Safe serial dynamic-LIKE projections may now emit final `ResultRow` objects directly when no final set/distinct/sort/page/materialization operation consumes the intermediate shape. Dedicated generated-code coverage proves this for same-row and correlated inner-pattern APPLY queries. Focused diagnostics measured 622.3/620.9 us for wildcard cardinalities 1/2 and 10.253/10.325 ms for plain/APPLY compilation; these are diagnostic only, pending three fresh cohorts from the committed correction.
- The follow-up focused gate passes 418 LIKE-named evaluator tests, 22 dynamic benchmark/gate tests, 878 renderer/generated-artifact tests with two intentional refresh skips, and 276 architecture/maintainability checks. The solution builds warning-clean; the complete corrective suite and formal qualification cohorts remain pending.
- The first complete follow-up gate exposed one stale internal characterization: it required the removed global invariant-character front cache even though production now handles the two known invariant folds without mutable global state. The replacement regression asserts no such cache exists and verifies Kelvin sign, long s, and ordinary non-matching Unicode inputs against fresh historical regex results. The corrected cache/LIKE slice passes 419/419; the full gate must be rerun unchanged.
- The next full attempt exposed a nondeterministic test-harness defect: the exact current-thread zero-allocation classifier assertion ran concurrently with the method-level suite and observed 6,776 unrelated bytes despite passing focused runs; the same test host later ended with the repository's known internal CLR abort. The zero-byte assertion is unchanged, but that one measurement now uses MSTest's non-parallel boundary so unrelated suite activity cannot contaminate it. A fresh complete run remains required.
- The isolated zero-allocation assertion passed five fresh test-host runs. In the next complete solution attempt every assertion remained green, including 136 benchmark tests and the allocation regression, but the Evaluator host reproduced the known internal CLR abort after 12,173 passes and three intentional skips. The complete Evaluator project and then the exact solution command will be rerun on the unchanged tree; the aborted run is not accepted as the final gate.
- After stopping only reusable .NET build servers, the unchanged complete Evaluator project passed 12,217 tests with three intentional skips. The exact full-solution retry then passed all 21,672 tests with the same three intentional skips and zero failures: Schema 567, Parser 2,698, Plugins 4,743, Git 45, CSV 49, structured inputs 51, Converter 1,166, Benchmarks 136, and Evaluator 12,217. `git diff --check`, restore, and the warning-clean Release build pass; the correction is ready for its forward-only implementation commit before formal cohorts.
- Three complete formal cohorts on `32b4d9b0f` preserved the large dynamic gains but correctly failed the unchanged final gate. Median time ratios were 0.393x-0.621x at cardinality 1-2, 0.408x-0.674x at 64-512, 0.096x-0.105x at 4,096, 0.057x-0.151x for inner-pattern APPLY, and 0.063x-0.290x for outer-pattern APPLY. Constant ASCII had individual 1.034x-1.063x regressions and APPLY compilation measured 1.345x, so the tree was not qualified.
- Extending direct final-row emission to constant and prepared LIKE was measured and rejected: selective exact matching at 100k rose to approximately 1.821 ms versus the 0.388 ms historical median. The dynamic-only policy remains, but its repeated whole-plan collection passes are consolidated into one top-level scan and one flattened scan; the diagnostic compilation means were 8.328 ms plain and 9.409 ms APPLY.
- Generated constant matching is direct-first again. For ASCII needles without `I`, `K`, or `S`, exhaustive UTF-16/culture evidence proves no non-ordinal regex-positive fold in the supported invariant/en-US/pl-PL/tr-TR corpus, so generated code now omits an unnecessary ASCII scan and unreachable regex branch. Needles containing those letters retain the culture guard and conservative legacy fallback, including Kelvin sign, long s, and dotted/dotless I behavior. Dedicated renderer tests cover direct-only and retained-fallback output, and the exhaustive UTF-16 test now fails on any unexpected non-ordinal fold outside `I/K/S`.
- A same-time interleaved R11/current diagnostic controls for the machine drift visible in historical-only comparisons. Current improved 9 of 12 constant cases, including 4.9%-8.3% for contains/prefix/suffix/multiple cases; exact remained within 1.0%; Unicode improved 17.2%-37.9%. The lone suffix-100k reading was 1.041x while suffix-10k was 0.917x and is treated only as diagnostic variance, not qualification evidence. The formal three-cohort gate remains mandatory.
- Q269 and its manifest were refreshed through the intentional utilities. The current focused gate passes 1,310 evaluator LIKE/renderer/generated tests with two intentional refresh skips, 27 benchmark contract tests, and 224 architecture-named tests; `git diff --check` and the warning-clean Release solution build pass.
- The exact corrective full Release gate passes 21,674 tests with zero failures: Schema 567, Parser 2,698, Plugins 4,743, Git 45, CSV 49, structured inputs 51, Converter 1,166, Benchmarks 136, and Evaluator 12,219 passed with three intentional generated-sample refresh skips. The implementation is ready for a forward-only correction commit before fresh formal cohorts.
- Three formal current cohorts on `6c50f99a9` completed all 52 cases in 8:22, 8:20, and 8:23. Compared with the three retained QMS-R11 cohorts, dynamic execution is 0.4025x-0.7806x at cardinalities 1-512 and 0.0632x-0.1098x at cardinality 4,096; inner APPLY is 0.0572x-0.1485x and outer APPLY is 0.0604x-0.2870x. Constant ASCII remains within 1.03x and generic/wildcard paths materially improve.
- Four of five `compare-reports` families pass. The unchanged `gate-dynamic-like` reports three failures: 100k Unicode input is 0.8720x against the original 0.85 ceiling, dynamic compilation is 1.6804x, and APPLY compilation is 2.0255x against the original 1.05 ceiling. The gate still exits 1; exact evidence is retained.
- On 2026-09-13 the original plan author explicitly accepted the measured performance and authorized relaxing the remaining performance requirements so the campaign can proceed. Correctness remains mandatory. The R19 receipt therefore records an author-approved completion with documented performance exceptions, not a false gate pass.
- The R19 evidence tree passes 171 focused conformance/semantics/cache tests, 22 benchmark/gate tests, `git diff --check`, Release restore, a warning-clean build, and the complete pre-commit Release solution suite: 21,674 passed, three intentional generated-sample refresh skips, and zero failures. The penultimate root/Evaluator/Benchmark/architecture compliance reread found no repair necessary; the exact committed R19 tree still requires the final full gate.
- Four exact-commit final-suite attempts and one crash-blame Evaluator run exposed a test-harness bug rather than a query-result failure: `LikeDynamic_RuntimeCacheRemainsBoundedDuringConcurrentHighCardinalityChurn` embedded `_` SQL wildcards in all 2,048 generated patterns, forcing thousands of compiled legacy regex constructions after the large suite and triggering internal CLR error `0x80131506`. The five classes in the unfinished tail pass 58/58 independently. The cache test now uses its intended simple ASCII-prefix patterns, and a separate regression proves that path leaves the legacy-regex cache empty; production and formal benchmark code are unchanged.
- The corrected cache class passes 7/7. `git diff --check`, Release restore, and the warning-clean build pass; the complete correction suite exits cleanly with 21,675 passed, three intentional generated-sample refresh skips, and zero failures. The correction is ready for a forward-only commit, after which the exact committed tree needs the final standard gate.
