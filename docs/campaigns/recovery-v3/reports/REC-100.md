# REC-100 — Enum member casing, aliases and nominal identity

## Result

REC-100 passed. The final replay covered 24 pre-registered candidate cases,
four validation controls, and one matrix check: 29 passed, 0 skipped, and 0
failed. No production implementation change was required.

## Authority and frozen matrix

The scope was bound to core specification sections 3.8, 18.4, 19.10, and
22.5, together with `docs/enums.md` and the active diagnostic catalog. The
matrix contains six exact-member-casing cases, six nominal-identity cases,
six unsupported-operator cases, and six duplicate-member cases. The controls
cover case-insensitive local type-name resolution, first-declared numeric-alias
canonicalization, native alias canonicalization, and exact named-composite
flags behavior.

The pre-observation artifacts were registered before candidate execution:

- `evidence/REC-100-pre-observation-manifest.json` — SHA-256
  `3af36c205d84d2691243891fe9679fc5f7ed2ad1685d9a9a87a53884308e8235`;
- `evidence/REC-100-case-contract.json` — SHA-256
  `ba8a2d002535bf86440f9d92e08e962504e94a00cf0f555c5e6b3c7be5653078`;
- parser test source — SHA-256
  `b9b43c96456cae63ef365b9723877466d1f9f280b24fb2eed3c042f4bb997d92`;
- evaluator semantic test source — SHA-256
  `000aedf8d2168569dca4064869c3adb9b787c8db3a4bea13673a01002645db06`;
- query-local control source — SHA-256
  `5432e01f5568e79ec469fc098c7e2813e3020fdc6684179b3755055190cdab2a`.

The first evaluator replay passed 10/22 and failed 12/22, so it was excluded
from candidate evidence. The failures were harness/oracle issues: the
semantic binder reports alias-free field spans, and the cross-source case used
the reserved alias `other`, producing `MQ2035_MissingRequiredAlias` before the
intended identity check. Revision 2 corrected only those expectations and
changed the alias to `peer`; no production code changed. The parser matrix
assertion also received a compile-only MSTest API correction before execution.
The contract was then re-frozen and `case_contract.py verify` passed.

## Diagnostic and valid-neighbor evidence

All 24 candidate cases were novel, with zero duplicates, zero metamorphic
variants, and a clean streak of 24. Each family passed all six cases with its
contracted root diagnostic and exact fault span:

- member casing: six `MQ3108_UnknownEnumMember` diagnostics;
- nominal identity: six `MQ3109_EnumIdentityMismatch` diagnostics;
- unsupported operators: six `MQ3110_UnsupportedEnumOperator` diagnostics;
- duplicate names: six `MQ2045_DuplicateEnumMember` diagnostics.

The candidate code counts are `MQ2045`: 6, `MQ3108`: 6, `MQ3109`: 6, and
`MQ3110`: 6. Every candidate had one root diagnostic, a valid location, a
catalog explanation and fix, and no unexpected secondary diagnostic. The
nominal-identity cases used distinct enum identities with equal backing values;
none was accepted as primitive integral equality, and suggestions preserved
the intended enum identity.

The four controls confirmed that local enum type names resolve without case
sensitivity, duplicate numeric values retain the first-declared canonical
member name, native aliases use that same canonical name, and a named
composite remains an exact flags member.

## Test evidence

Final focused replay:

```text
dotnet test src/dotnet/Musoq.Parser.Tests/Musoq.Parser.Tests.csproj --configuration Release --no-build --no-restore --nologo --verbosity minimal --filter "FullyQualifiedName~DiagnosticREC100EnumDeclarationTests" --logger "console;verbosity=minimal"
dotnet test src/dotnet/Musoq.Evaluator.Tests/Musoq.Evaluator.Tests.csproj --configuration Release --no-build --no-restore --nologo --verbosity minimal --filter "FullyQualifiedName~DiagnosticREC100EnumSemanticsTests|FullyQualifiedName~DiagnosticREC100_QueryLocal" --logger "console;verbosity=minimal"
```

Result: parser 8/8 and evaluator 22/22 passed. Adjacent parser suites passed
238/238, adjacent evaluator suites passed 194/194, and campaign tools passed
46/46.

## Full gate

The fresh unfiltered Release gate passed all eight expected solution targets.
It recorded 20,239 passed, three established allowlisted refresh skips, zero
failed, and 20,242 total tests.

- Run ID: `REC-100-e3cdbdbb801e`.
- Tested payload:
  `ae95835f9c4f652b95e11545dead981a58d7beeaa411baef3663a510c73ddd5b` across
  5,752 files.
- Full-gate evidence: `evidence/REC-100-full-gate.json`.
- Full-gate evidence SHA-256:
  `90db20ab937ed6f06ae00cb583c3c922bb6d2bf789ed26330115307fd82504eb`.
- SDK observed: `10.0.303`.
- Validation baseline commit:
  `1469ebece68ae3139706767be3e4111e3aa8b34f`.

The three skips are the established generated-interpretation and generated-code
refresh tests from the REC-085 allowlist; no additional skip was accepted.

## Obligation mapping

- REC-100-O01: the six exact-member-casing candidates and the two local
  type-name controls separate case-insensitive type lookup from exact member
  lookup.
- REC-100-O02: six exact/case-only duplicate-name candidates produce MQ2045,
  while the numeric-alias control preserves the first-declared canonical name.
- REC-100-O03: native alias output uses the first-declared canonical name and
  the named-composite control preserves exact flags-member behavior.
- REC-100-O04: six equal-backing-value cross-identity candidates produce
  MQ3109, proving nominal identity is not reduced to integral equality.

The scope acceptance is met: diagnostic suggestions preserve enum identity and
do not select a similarly named member from another enum.

## Self-review

The main session reviewed the scope authority and REC-099 dependency,
pre-observation registration and revision history, compile-only correction,
type/member case rules, duplicate names and aliases, nominal identity and
operator contracts, catalog code and span contracts, valid alias/composite
controls, focused results, and fresh full-gate evidence. The review was
self-review; no independent reviewer was available. No additional finding was
identified. Product and empirical lanes remain outside the active engine-lane
stopping contract.
