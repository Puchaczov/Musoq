# REC-093 — Ambiguity, missing information and justified abstention

## Result

REC-093 passed. The scope added evaluator diagnostics tests only; no production
implementation change was required.

The final frozen replay passed all 48 candidate cases, all four validation
controls, and the matrix-shape check: 53 passed, 0 skipped, and 0 failed.
Unique close spellings remained repairable, while ambiguous, missing-metadata,
unsupported, and genuinely out-of-scope cases did not receive falsely unique
automatic fixes.

## Frozen contract

The final matrix is revision 5 and was registered before the final replay.

- 48 candidates: 12 unique justified spellings, 12 equally plausible visible
  candidates, 12 out-of-scope cases, and 12 missing-or-unsupported cases.
- Four non-counted controls verify foreign-provider isolation, bounded and stable
  candidate lists, missing-metadata explanation, and unsupported-syntax root
  classification.
- Each candidate has one expected root diagnostic, expected phase, and an
  asserted explanation and documentation reference.
- Candidate facts are bounded to five or fewer entries; the largest observed
  list contains two tied candidates.
- The 12 unique spelling cases emit one `TextEdit` each, and applying each edit
  produces a query that analyzes successfully.
- The 12 ambiguous cases emit no structured edit. The six empty-metadata cases
  remain Bind diagnostics with no edit, and the six unsupported-syntax cases
  remain Parse diagnostics with no edit.
- The three `Nami` cases have one in-scope Basic `Name` candidate and retain the
  justified unique-edit path. The other nine out-of-scope cases abstain, and
  names exposed only by the foreign provider do not leak into Basic candidate
  facts.

Frozen artifacts:

- `docs/campaigns/recovery-v3/evidence/REC-093-pre-observation-manifest.json` —
  SHA-256 `501ad0bd4217f564de7f1368069159aa48c1a3936dd7045fe02458ae70e779ef`.
- `docs/campaigns/recovery-v3/evidence/REC-093-case-contract.json` — SHA-256
  `ed3105803ecdf1292fe2045df7f6b67ac532b9af34cb6c780a914aa3b2f71711`.
- Test source SHA-256
  `0f5aa93cd38fa30fa3c21605d892d7d77fb690a3b921c666e6fac6247bff8279`.

## Observation and corrections

The first executable iteration was deliberately excluded from candidate
evidence: 38 passed, 15 failed, and 53 total. It exposed the real Basic-library
`IsAlpha` candidate, deterministic callable ordering, one repeated callable
typo, repeated missing-metadata references, and the parser's MQ2020
classification for the unsupported `^` operator.

The corrected revision-4 replay passed 50 and failed three of 53 cases. The
only remaining failures were an over-specific message assertion for MQ2020;
the diagnostic classification was already correct, but its stable catalog
message describes a missing binary-operator operand without echoing `^`.

Revision 5 changed that assertion to the stable `binary operator` explanation.
This was an assertion-only correction after the candidate matrix had already
been observed; the revision-4 replay remains retained and excluded from
candidate evidence. The final replay was then run from a rebuilt assembly.

## Test evidence

Final focused replay:

```text
dotnet build src/dotnet/Musoq.Evaluator.Tests/Musoq.Evaluator.Tests.csproj --configuration Release --no-restore --no-incremental --nologo --verbosity minimal
dotnet test src/dotnet/Musoq.Evaluator.Tests/Musoq.Evaluator.Tests.csproj --configuration Release --no-build --no-restore --nologo --verbosity minimal --filter "FullyQualifiedName~DiagnosticREC093AmbiguityAbstentionTests" --logger "console;verbosity=minimal"
```

Result: 53 passed, 0 skipped, 0 failed, 53 total. This is 48 candidates, four
controls, and one matrix-shape check.

Adjacent evaluator suites:

```text
dotnet test src/dotnet/Musoq.Evaluator.Tests/Musoq.Evaluator.Tests.csproj --configuration Release --no-build --no-restore --nologo --verbosity minimal --filter "FullyQualifiedName~CompilationPipelineErrorTests|FullyQualifiedName~CallableResolutionDiagnosticsTests|FullyQualifiedName~AliasTests|FullyQualifiedName~ColumnsTests|FullyQualifiedName~DiagnosticExceptionTaxonomyTests" --logger "console;verbosity=minimal"
```

Result: 149 passed, 0 skipped, 0 failed, 149 total.

Adjacent parser suites:

```text
dotnet test src/dotnet/Musoq.Parser.Tests/Musoq.Parser.Tests.csproj --configuration Release --no-build --no-restore --nologo --verbosity minimal --filter "FullyQualifiedName~DiagnosticREC091MissingTokenTargetingTests|FullyQualifiedName~DiagnosticRuntime060EnvelopeSafetyTests|FullyQualifiedName~DiagnosticUx064DiagnosticContractTests|FullyQualifiedName~DiagnosticCoreTypesTests" --logger "console;verbosity=minimal"
```

Result: 103 passed, 0 skipped, 0 failed, 103 total.

Campaign tool tests:

```text
python -m unittest discover -s docs/campaigns/recovery-v3/tools -p "test_*.py" -v
```

Result: 46 passed, 0 skipped, 0 failed, 46 total.

## Full gate

The fresh unfiltered Release gate passed all eight expected targets. It recorded
20,010 passed, three established allowlisted skips, zero failed, and 20,013
total tests. Restore, no-incremental Release build, and unfiltered Release test
commands all exited with code 0.

- Run ID: `REC-093-3afdda4c434e`.
- Tested payload:
  `c80de68090d8a131638e9eacffaf95656594c2594fc56dae89b930ff9f958628` across
  5,742 files.
- Full-gate evidence:
  `docs/campaigns/recovery-v3/evidence/REC-093-full-gate.json`.
- Full-gate evidence SHA-256:
  `53040c3610e3c1f600597b010d3aa58f726067038a20689e90585a603a7332c4`.
- SDK observed: `10.0.303`.
- Validation baseline commit:
  `1a9ef14584334eeefe835713fb1ab595a7953232`.

The three skips are the established generated-interpretation and generated-code
refresh tests from the REC-085 allowlist; no additional skip was accepted.

## Obligation mapping

- REC-093-O01: evidenced by the four 12-case families, tied visible columns,
  aliases, properties, and callables, foreign-provider names, unrelated
  sources, and empty metadata.
- REC-093-O02: evidenced by the truthful bounded candidate facts and the
  unique-edit-only policy. All 12 ambiguous cases and all unsupported or
  missing-metadata cases abstained.
- REC-093-O03: evidenced by the distinct Bind/Parse root phases and the
  MQ3001, MQ3015, MQ3028, MQ3085, MQ3086, MQ1001, and MQ2020 code assertions.
- REC-093-O04: evidenced by the 12 ambiguous no-edit cases with retained
  explanations and documentation references, alongside 12 unique spelling
  repairs and three unique in-scope `Name` repairs.

The scope acceptance is met: no ambiguous case received a falsely unique
correction, and unique justified typo fixes remained available and analyzable.

## Self-review

The main session reviewed the scope diff, final matrix revision and source hash,
provider profiles, diagnostic codes and phases, candidate bounds, edit policy,
excluded observation history, focused results, adjacent suites, and fresh
full-gate evidence. No production fix or finding was identified. Product and
empirical lanes remain outside this active engine-lane scope.
