# REC-098 — Enum declaration grammar and borrowed forms

## Result

REC-098 passed. The final replay covered 24 registered malformed declaration
cases, four validation controls, and one matrix check: 28 passed, 0 skipped,
and 0 failed. No production implementation change was required.

## Authority and frozen matrix

The scope was bound to the current core specification sections 3.8, 18.4,
19.10, 22.5, 23.4, and 2.2, together with `docs/enums.md` and catalog entries
MQ2042–MQ2048. The matrix has six four-case families: structural omissions,
backing/value errors, duplicate/empty declarations, member grammar, borrowed
dialect forms, and contextual boundaries.

The source and expectation artifacts were registered before candidate
execution. Two compile-only harness corrections occurred before any malformed
query ran: the DynamicData record was made public, then nullable root
assertions and current MSTest analyzer requirements were corrected. Neither
changed a candidate expectation.

The first executable replay passed 27/28 but was excluded. Its one failure
showed that an unclosed member body enters the existing MQ2042 structural
separator branch at a zero-length EOF span, not the initially expected MQ2017
EOF branch. The source branch, core diagnostic table, and catalog were
reviewed; revision 2 corrected only that expectation, recorded the oracle
history, and was frozen before the final replay.

Frozen artifacts:

- `evidence/REC-098-pre-observation-manifest.json` — SHA-256
  `520df9f62996e2279a19c2fa38b5b048794c21ae4ecb0e1f6ea8a7ee2c0eba93`;
- `evidence/REC-098-case-contract.json` — SHA-256
  `b80f916a90733effabb692a77dc70ce01e191ffe4c3bf27e1d11e1e845fb57f0`;
- test source SHA-256
  `fca3bc87f6e70b12dcecbc740cd7253743e46be365f270595065f820b9fadf56`.

## Diagnostic and valid-neighbor evidence

The final matrix produced 24 novel candidates, zero duplicates, zero
metamorphic variants, and a clean streak of 24. It emitted one root
diagnostic per candidate with valid locations and exact smallest fault spans.
The code counts were MQ2001: 1, MQ2042: 9, MQ2043: 1, MQ2044: 5, MQ2045: 2,
MQ2046: 3, MQ2047: 1, and MQ2048: 2.

The controls preserve canonical and flags declarations, all eight integral
backing types at boundaries, trailing commas, duplicate-value aliases,
contextual `enum`/`flags` identifiers, and an executable statement following
an enum declaration. MySQL and PostgreSQL borrowed forms remain explicitly
rejected; C# implicit member values and access-modified declarations remain
invalid at their contracted parser boundaries.

## Test evidence

Final focused replay:

```text
dotnet build src/dotnet/Musoq.Parser.Tests/Musoq.Parser.Tests.csproj --configuration Release --no-restore --no-incremental --nologo --verbosity minimal
dotnet test src/dotnet/Musoq.Parser.Tests/Musoq.Parser.Tests.csproj --configuration Release --no-build --no-restore --nologo --verbosity minimal --filter "FullyQualifiedName~DiagnosticREC098EnumDeclarationRecoveryTests" --logger "console;verbosity=minimal"
```

Result: 28 passed, 0 skipped, 0 failed, 28 total. The adjacent parser suites
passed 222/222, and campaign tools passed 46/46.

## Full gate

The fresh unfiltered Release gate passed all eight expected targets. It
recorded 20,180 passed, three established allowlisted refresh skips, zero
failed, and 20,183 total tests.

- Run ID: `REC-098-13f22542ad88`.
- Tested payload:
  `57fd88e6fe87995ca61e1e650022de08c318152492b750fa9ea30585274039db` across
  5,747 files.
- Full-gate evidence: `evidence/REC-098-full-gate.json`.
- Full-gate evidence SHA-256:
  `8f9f90f2ecea7a2494fcf3403927ad293ffba9cac8d180a620d428e2f7aa01c3`.
- SDK observed: `10.0.303`.
- Validation baseline commit:
  `e420364695e16ded3359cc2485f324920f4ff7ba`.

The three skips are the established generated-interpretation and
generated-code refresh tests from the REC-085 allowlist; no additional skip
was accepted.

## Obligation mapping

- REC-098-O01: the matrix covers the required declaration omissions, member
  syntax, explicit values, duplicate/empty forms, and valid-neighbor cases.
- REC-098-O02: all eight integral backing types and MQ2042–MQ2048 code/span
  expectations are exercised against the current catalog.
- REC-098-O03: PostgreSQL, MySQL, and C#-like borrowed forms are explicitly
  invalid and do not broaden the accepted Musoq grammar.
- REC-098-O04: each expected declaration fault is pinned to its smallest
  parser span; the observed EOF branch correction is preserved and replayed.

The scope acceptance is met: every contracted enum parser family has both
trigger coverage and valid-neighbor protection.

## Self-review

The main session reviewed the authority and dependency, pre-observation
registration, compile-only corrections, enum grammar and ranges, dialect
boundaries, catalog code/span contract, valid-neighbor controls, focused
results, and fresh full-gate evidence. No additional finding was identified.
Product and empirical lanes remain outside the active engine-lane stopping
contract.
