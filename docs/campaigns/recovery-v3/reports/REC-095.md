# REC-095 — Multiple actions, overlapping edits and repeated application

## Result

REC-095 passed. The scope added a test-local composition harness and evaluator
tests only; no production implementation or native multi-edit behavior was
changed.

The final replay passed all 24 composition candidates, all four validation
controls, and the matrix check: 29 passed, 0 skipped, and 0 failed. Independent
actions compose against one original coordinate system, conflicts are rejected
without an arbitrary order, and old actions are rejected after the document
changes.

## Frozen contract

The final matrix is revision 2 and was frozen before candidate observation.
Revision 1 was superseded before observation by a compile-only namespace import.

- 24 candidates cover four non-overlapping batches, four length-changing
  original-coordinate batches, four overlapping conflicts, four mutually
  exclusive alternatives, four repeated applications, and four reanalyze-after-
  each-action sequences.
- The independent batches use two real native diagnostic edits from one query.
  The length-changing batches replace the first typo with valid `Country` while
  applying the second edit in its original coordinates.
- Overlapping spans and alternative replacements are rejected explicitly; no
  destructive application order is selected and the source document remains
  unchanged.
- Reapplying an old edit after successful correction is rejected by stale
  identity, including idempotent and same-length current-text cases.
- Sequential cases reject the old second action, reanalyze current text, then
  apply the refreshed second action successfully.
- The native contract remains individual `DiagnosticAction` and `TextEdit`
  values. The test harness composes them; the engine is not required to expose
  a batch-edit field.

Frozen artifacts:

- `docs/campaigns/recovery-v3/evidence/REC-095-pre-observation-manifest.json` —
  SHA-256 `24a31a9e3e8422473bc29de4057ae69daa4a82bc38709b356d82ff0e86430142`.
- `docs/campaigns/recovery-v3/evidence/REC-095-case-contract.json` — SHA-256
  `2b7b851d4f3670ce5fa65de1f781b07b9e1088a1477f8f12422fc79a54017ef`.
- Test source SHA-256
  `1ce7dd2a1e32c19eb0d2feb39cc48fb1406a4f9ab629b53a6a2d6bc952ee65b5`.

## Observation and correction

The first frozen source failed compilation before candidate observation because
the overlap cases constructed `TextSpan` without importing `Musoq.Parser`.
Revision 2 added that namespace only; candidate cases and expectations were
unchanged. The corrected source built with zero warnings and zero errors before
the first executable replay.

## Test evidence

Final focused replay:

```text
dotnet build src/dotnet/Musoq.Evaluator.Tests/Musoq.Evaluator.Tests.csproj --configuration Release --no-restore --no-incremental --nologo --verbosity minimal
dotnet test src/dotnet/Musoq.Evaluator.Tests/Musoq.Evaluator.Tests.csproj --configuration Release --no-build --no-restore --nologo --verbosity minimal --filter "FullyQualifiedName~DiagnosticREC095MultipleActionsTests" --logger "console;verbosity=minimal"
```

Result: 29 passed, 0 skipped, 0 failed, 29 total. This is 24 candidates, four
controls, and one matrix check.

Adjacent evaluator suites:

```text
dotnet test src/dotnet/Musoq.Evaluator.Tests/Musoq.Evaluator.Tests.csproj --configuration Release --no-build --no-restore --nologo --verbosity minimal --filter "FullyQualifiedName~DiagnosticREC093AmbiguityAbstentionTests|FullyQualifiedName~DiagnosticREC094DocumentIdentityTests|FullyQualifiedName~DiagnosticREC095MultipleActionsTests|FullyQualifiedName~CompilationPipelineErrorTests|FullyQualifiedName~CallableResolutionDiagnosticsTests|FullyQualifiedName~AliasTests|FullyQualifiedName~ColumnsTests|FullyQualifiedName~DiagnosticExceptionTaxonomyTests" --logger "console;verbosity=minimal"
```

Result: 260 passed, 0 skipped, 0 failed, 260 total.

Adjacent parser suites:

```text
dotnet test src/dotnet/Musoq.Parser.Tests/Musoq.Parser.Tests.csproj --configuration Release --no-build --no-restore --nologo --verbosity minimal --filter "FullyQualifiedName~DiagnosticREC091MissingTokenTargetingTests|FullyQualifiedName~DiagnosticRuntime060EnvelopeSafetyTests|FullyQualifiedName~DiagnosticUx064DiagnosticContractTests|FullyQualifiedName~DiagnosticCoreTypesTests|FullyQualifiedName~DiagnosticMetadataAndPhasesTests" --logger "console;verbosity=minimal"
```

Result: 127 passed, 0 skipped, 0 failed, 127 total.

Campaign tool tests:

```text
python -m unittest discover -s docs/campaigns/recovery-v3/tools -p "test_*.py" -v
```

Result: 46 passed, 0 skipped, 0 failed, 46 total.

## Full gate

The fresh unfiltered Release gate passed all eight expected targets. It recorded
20,068 passed, three established allowlisted skips, zero failed, and 20,071
total tests. Restore, no-incremental Release build, and unfiltered Release test
commands all exited with code 0.

- Run ID: `REC-095-afad8fc0858a`.
- Tested payload:
  `3cf2f856ef2de01d78adbd7357e12ee20d9b2c1d922629172074e9d69bbd92a0` across
  5,744 files.
- Full-gate evidence:
  `docs/campaigns/recovery-v3/evidence/REC-095-full-gate.json`.
- Full-gate evidence SHA-256:
  `f4d707704f93cc1f988de34ab47a01dc8199228d18ce11e8a71438c80cff7fae`.
- SDK observed: `10.0.303`.
- Validation baseline commit:
  `b57de32c99016fcbf314e1116733a06a9b443515`.

The three skips are the established generated-interpretation and generated-code
refresh tests from the REC-085 allowlist; no additional skip was accepted.

## Obligation mapping

- REC-095-O01: evidenced by the four independent batches, four overlapping
  span sets, and four mutually exclusive alternative sets.
- REC-095-O02: evidenced by descending original-offset application for
  independent actions and four sequential cases that reject the stale second
  action before reanalysis supplies a fresh coordinate.
- REC-095-O03: evidenced by eight explicit conflict rejections with zero
  destructive orders selected and zero original-document mutations.
- REC-095-O04: evidenced by four safe old-action rejections after correction,
  plus four successful fresh actions after reanalysis.

The scope acceptance is met: the harness catches offset corruption and
conflicting edits without requiring native multi-edit support.

## Self-review

The main session reviewed the scope diff, frozen matrix and source hash, native
action boundary, independent and length-changing coordinate application,
overlap and alternative conflict policy, repeated and reanalysis behavior,
focused results, adjacent suites, and fresh full-gate evidence. No production
fix or finding was identified. Product and empirical lanes remain outside this
active engine-lane scope.
