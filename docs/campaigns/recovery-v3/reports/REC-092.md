# REC-092 — Meaning-preserving repairs with discriminating fixtures

## Result

REC-092 passed. The scope added evaluator tests only; no production implementation change was required.

The final frozen replay executed 48 candidate pairs. Each gold query and each deliberately meaning-changing repaired query compiled and ran. All 48 gold snapshots matched their registered shape and expected results, all 48 repaired snapshots matched their registered non-equivalent results, and all 48 non-equivalence assertions passed.

## Frozen contract

The final matrix is revision 4 and was registered before the final candidate replay.

- 48 candidates: 12 dropped-filter, 12 added-DISTINCT, 12 changed-join, and 12 replaced-constant cases.
- Four non-counted controls cover invalid and valid strict-versus-soft conversion and Parse-versus-TryParse behavior.
- Fixture kinds are intentionally non-empty and discriminating: `FilterRows`, `FilterRowsWithDuplicates`, `DistinctRows`, `DistinctRowsAlt`, `JoinRows`, `SmallJoinRows`, `ConstantRows`, and `ConstantRowsAlt`.
- Every snapshot checks column names and CLR types, row values including NULL, and row multiplicity.
- The 45 unordered cases use bag comparison; the three explicitly ordered cases use sequence comparison.
- Two cases bind script parameters and three cases assert explicit ordering and result-level slicing.
- Duplicate rows, NULL values, outer-join extension, set-operation multiplicity, and boundary constants are observable in the registered fixtures.

Frozen artifacts:

- `docs/campaigns/recovery-v3/evidence/REC-092-pre-observation-manifest.json` — SHA-256 `9ccb8214b1f43fafb9fea62a46e23a619a1aa27730687b5a96270a6a4f868ac6`.
- `docs/campaigns/recovery-v3/evidence/REC-092-case-contract.json` — SHA-256 `de99001bf5e6906fe97a2a23f005efbb8b01d5b96b71143948efaa0a9f1545b2`.
- Test source SHA-256 `893d9329ce2733f167f412a61dee645d1e6c1209df7ac93c74f6de1c41d4c368`.

## Observation and corrections

The first executable iteration was deliberately excluded from candidate evidence: 49 passed, 4 failed, and 53 total. It exposed the expected SQL three-valued behavior of `NOT (Country = 'PL')`, UNION ALL preservation of both operand NULL rows and duplicates, and the concrete `ParseException` failure type.

A subsequent no-build replay was also excluded because it used the pre-correction assembly. The corrected expectations and control assertion were rebuilt before the final replay. Self-review then identified that the replaced-constant family needed a second fixture kind to satisfy O01 explicitly; `ConstantRowsAlt` was added to two cases. Revision 4 was frozen before the final replay.

## Test evidence

Final focused replay:

```text
dotnet build src/dotnet/Musoq.Evaluator.Tests/Musoq.Evaluator.Tests.csproj --configuration Release --no-restore --no-incremental --nologo --verbosity minimal
dotnet test src/dotnet/Musoq.Evaluator.Tests/Musoq.Evaluator.Tests.csproj --configuration Release --no-build --no-restore --nologo --verbosity minimal --filter "FullyQualifiedName~DiagnosticREC092MeaningPreservationTests" --logger "console;verbosity=minimal"
```

Result: 53 passed, 0 skipped, 0 failed, 53 total. This is 48 candidates, four controls, and one matrix-shape check.

Adjacent semantic suites:

```text
dotnet test src/dotnet/Musoq.Evaluator.Tests/Musoq.Evaluator.Tests.csproj --configuration Release --no-build --no-restore --nologo --verbosity minimal --filter "FullyQualifiedName~Core013NullComparisonSemanticsTests|FullyQualifiedName~Core027SetOperationTests|FullyQualifiedName~Core028OrderByTests|FullyQualifiedName~Core016JoinSemanticsTests|FullyQualifiedName~Core014PredicateSubqueryTests|FullyQualifiedName~PostfixCastEvaluatorTests|FullyQualifiedName~DiagnosticCore007ScriptParameterTests|FullyQualifiedName~DiagnosticInterpret054ApplyNullSemanticsTests" --logger "console;verbosity=minimal"
```

Result: 67 passed, 0 skipped, 0 failed, 67 total.

Campaign tool tests:

```text
python -m unittest discover -s docs/campaigns/recovery-v3/tools -p "test_*.py" -v
```

Result: 46 passed, 0 skipped, 0 failed, 46 total.

## Full gate

The fresh unfiltered Release gate passed all eight expected targets. It recorded 19,960 passed, three established allowlisted skips, zero failed, and 19,963 total tests. Restore, no-incremental Release build, and unfiltered Release test commands all exited with code 0.

- Run ID: `REC-092-fabb2f6ec737`.
- Tested payload: `420f7afbbf0f247eff89b84961f27ea9360a4cd25ce541a372e79d23282c5926` across 5,741 files.
- Full-gate evidence: `docs/campaigns/recovery-v3/evidence/REC-092-full-gate.json`.
- Full-gate evidence SHA-256: `69fb4adba376bdbc5655977ab55ff64cf4be7ce00d95e663adeb93bd4dc8bfad`.
- SDK observed: `10.0.303`.
- Validation baseline commit: `5a4966998d0ef54b4857de287dd83fcc7a73cdde`.

The three skips are the established generated-interpretation and generated-code refresh tests from the REC-085 allowlist; no additional skip was accepted.

## Obligation mapping

- REC-092-O01: evidenced by the frozen matrix, its four 12-case families, and the eight fixture kinds; each family has multiple discriminating fixture kinds.
- REC-092-O02: evidenced by the 96 shape/type snapshot checks, explicit NULL and duplicate assertions, two parameter cases, and three ordered slicing cases.
- REC-092-O03: evidenced by 90 unordered bag snapshot comparisons and six ordered sequence snapshot comparisons.
- REC-092-O04: evidenced by four policy controls: invalid strict cast, valid strict/soft agreement, invalid Parse failure versus TryParse NULL behavior, and valid Parse/TryParse agreement.

The scope acceptance is met because the deliberately changed repairs compiled and ran but were rejected as semantically equivalent in all 48 cases, and no empty fixture was used as proof.

## Self-review

The main session reviewed the scope diff, final matrix revision and source hash, fixture diversity, expected-result corrections, policy controls, focused results, adjacent results, and fresh full-gate evidence. No production fix or finding was identified. Product and empirical lanes remain outside this active engine-lane scope.
